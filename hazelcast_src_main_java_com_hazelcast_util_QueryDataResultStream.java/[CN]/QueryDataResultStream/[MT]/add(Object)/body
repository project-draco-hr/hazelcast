{
  if (!started) {
    started=true;
  }
  QueryResultEntry entry=(QueryResultEntry)obj;
  if (!set || keys.add(entry.getIndexKey())) {
    q.offer(entry);
    size++;
    return true;
  }
  return false;
}
