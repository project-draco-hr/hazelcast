{
  try {
    return Integer.parseInt(value);
  }
 catch (  Exception e) {
    System.out.println(parameterName + " parameter value, [" + value+ "], is not a proper integer. Default value, ["+ defaultValue+ "], will be used!");
    e.printStackTrace();
    return defaultValue;
  }
}
