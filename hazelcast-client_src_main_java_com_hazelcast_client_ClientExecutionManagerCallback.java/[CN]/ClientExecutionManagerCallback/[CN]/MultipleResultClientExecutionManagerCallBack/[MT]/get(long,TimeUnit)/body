{
  if (result == null) {
synchronized (this) {
      if (result == null) {
        Packet packet;
        if (l < 0) {
          packet=queue.take();
        }
 else {
          packet=queue.poll(l,timeUnit);
        }
        this.result=handleResult(packet);
      }
      if (it == null) {
        it=result.iterator();
      }
    }
  }
  if (it.hasNext()) {
    return it.next();
  }
 else {
    return OBJECT_DONE;
  }
}
