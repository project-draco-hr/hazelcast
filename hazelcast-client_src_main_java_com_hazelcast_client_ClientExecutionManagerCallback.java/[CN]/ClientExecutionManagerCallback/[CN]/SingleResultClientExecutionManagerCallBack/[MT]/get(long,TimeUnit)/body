{
  if (done) {
    return result;
  }
synchronized (this) {
    if (!done) {
      Packet packet;
      if (l < 0) {
        packet=queue.take();
      }
 else {
        packet=queue.poll(l,timeUnit);
      }
      this.result=handleResult(packet);
      done=true;
    }
    return result;
  }
}
