{
  queue.setConsumerThread(Thread.currentThread());
  queue.put("item1");
  queue.put("item2");
  queue.put("item3");
  queue.take();
  queue.take();
  queue.take();
  assertEquals(0,queue.size());
}
