{
  return new AbstractOperation(){
{
      setPartitionId(0);
    }
    @Override public void run() throws Exception {
      if (fail) {
        throw new TransactionException();
      }
    }
  }
;
}
