{
  ISet set=newSet();
  Set setTest=new HashSet();
  for (int i=1; i <= 10; i++) {
    set.add("item" + i);
    setTest.add("item" + i);
  }
  set.removeAll(setTest);
  assertSizeEventually(0,set);
}
