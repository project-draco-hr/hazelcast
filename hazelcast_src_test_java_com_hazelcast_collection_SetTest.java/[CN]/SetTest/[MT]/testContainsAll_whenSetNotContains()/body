{
  ISet set=newSet();
  Set setTest=new HashSet();
  setTest.add("item1");
  setTest.add("item100");
  for (int i=1; i <= 10; i++) {
    set.add("item" + i);
    setTest.add("item" + i);
  }
  assertFalse(set.containsAll(setTest));
}
