{
  ExpiryMarker marker=new ExpiryMarker(null,150L,"the-marker-id").expire(100L);
  assertFalse("marker is not replaceable when it when timestamp equal to expiry",marker.isReplaceableBy(100L,null,null));
}
