{
  ExpiryMarker marker=new ExpiryMarker(null,150L,"the-marker-id").expire(100L);
  assertFalse("marker is not replaceable when it when timestamp before expiry",marker.isReplaceableBy(99L,null,null));
}
