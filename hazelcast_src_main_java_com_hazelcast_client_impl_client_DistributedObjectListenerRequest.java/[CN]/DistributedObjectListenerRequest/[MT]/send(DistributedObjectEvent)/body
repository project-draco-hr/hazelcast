{
  if (endpoint.isAlive()) {
    PortableDistributedObjectEvent portableEvent=new PortableDistributedObjectEvent(event.getEventType(),(String)event.getObjectId(),event.getServiceName());
    endpoint.sendEvent(null,portableEvent,getCallId());
  }
}
