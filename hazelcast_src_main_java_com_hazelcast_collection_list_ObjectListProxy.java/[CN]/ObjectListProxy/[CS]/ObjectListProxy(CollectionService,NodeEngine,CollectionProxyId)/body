{
  super(service,nodeEngine,nodeEngine.getConfig().getMultiMapConfig(COLLECTION_LIST_NAME + proxyId.getKeyName()).setValueCollectionType(MultiMapConfig.ValueCollectionType.LIST),proxyId);
  key=nodeEngine.toData(proxyId.getKeyName());
}
