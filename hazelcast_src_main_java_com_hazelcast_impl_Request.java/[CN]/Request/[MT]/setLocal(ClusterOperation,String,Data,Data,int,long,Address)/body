{
  reset();
  set(true,operation,name,key,value,blockId,timeout,-1,-1,-1,thisAddress,0,thisAddress,-1,-1);
  this.txnId=ThreadContext.get().getTxnId();
  this.lockThreadId=ThreadContext.get().getThreadId();
  this.caller=thisAddress;
}
