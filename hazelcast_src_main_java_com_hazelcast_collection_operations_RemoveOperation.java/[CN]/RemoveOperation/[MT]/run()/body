{
  Collection coll=getCollection();
  if (coll == null) {
    response=false;
    return;
  }
  Object obj=isBinary() ? value : toObject(value);
  response=coll.remove(obj);
  if (coll.isEmpty()) {
    removeCollection();
  }
}
