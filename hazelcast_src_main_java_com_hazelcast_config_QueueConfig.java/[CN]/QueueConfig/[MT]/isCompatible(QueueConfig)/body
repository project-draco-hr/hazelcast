{
  if (queueConfig == null)   return false;
  return (name != null ? name.equals(queueConfig.name) : queueConfig.name == null) && this.backingMapName.equals(queueConfig.backingMapName) && this.maxSizePerJVM == queueConfig.maxSizePerJVM;
}
