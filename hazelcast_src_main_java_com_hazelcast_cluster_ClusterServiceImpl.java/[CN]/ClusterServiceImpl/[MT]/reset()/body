{
  lock.lock();
  try {
    joinInProgress=false;
    setJoins.clear();
    timeToStartJoin=0;
    membersRef.set(null);
    masterConfirmationTimes.clear();
  }
  finally {
    lock.unlock();
  }
}
