{
  lock.lock();
  try {
    if (migratingPartition != null) {
      if (deadAddress.equals(migratingPartition.getFromAddress()) || deadAddress.equals(migratingPartition.getToAddress())) {
        migratingPartition=null;
      }
    }
  }
  finally {
    lock.unlock();
  }
}
