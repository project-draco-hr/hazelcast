{
  if (list == null || list.isEmpty()) {
    return WriteBehindQueues.emptyWriteBehindQueue();
  }
  return new BoundedArrayWriteBehindQueue(new ArrayList<DelayedEntry>(list),maxSize,writeBehindQueueItemCounter);
}
