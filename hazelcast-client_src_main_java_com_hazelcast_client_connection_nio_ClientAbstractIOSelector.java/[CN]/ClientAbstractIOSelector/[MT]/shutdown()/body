{
  selectorQueue.clear();
  try {
    addTask(new Runnable(){
      public void run(){
        live=false;
        shutdownLatch.countDown();
      }
    }
);
    interrupt();
  }
 catch (  Throwable ignored) {
  }
}
