{
  final R record=createRecord(value,expirationTime);
  updateHasExpiringEntry(record);
  if (isEventsEnabled) {
    publishEvent(CacheEventType.CREATED,keyData,null,toEventData(value),false,completionId,expirationTime,origin);
  }
  return record;
}
