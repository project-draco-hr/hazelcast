{
  if (!cacheConfig.isStatisticsEnabled()) {
    return false;
  }
  if (statistics == null) {
synchronized (this) {
      if (statistics == null) {
        this.statistics=cacheService.createCacheStatIfAbsent(name);
      }
    }
  }
  return true;
}
