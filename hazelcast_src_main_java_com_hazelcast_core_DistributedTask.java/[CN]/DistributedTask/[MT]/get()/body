{
  inner.get();
  if (cancelled)   throw new CancellationException();
  if (exception != null)   throw new ExecutionException(exception);
  if (memberLeft)   throw new MemberLeftException();
  return result;
}
