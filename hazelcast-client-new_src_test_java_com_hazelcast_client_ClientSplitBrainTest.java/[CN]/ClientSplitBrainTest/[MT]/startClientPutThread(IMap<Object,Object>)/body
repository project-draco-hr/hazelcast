{
  final Thread clientThread=new Thread(){
    @Override public void run(){
      while (!Thread.interrupted()) {
        mapClient.put(1,1);
      }
    }
  }
;
  clientThread.start();
  return clientThread;
}
