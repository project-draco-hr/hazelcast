{
  if (in.readBoolean()) {
    int size=in.readInt();
    LinkedList result=new LinkedList();
    for (int i=0; i < size; i++) {
      result.add(i,in.readObject());
    }
    return result;
  }
  return null;
}
