{
  mapService.interceptAfterGet(name,result);
  if (mapContainer.getMapConfig().isStatisticsEnabled()) {
    mapContainer.getLocalMapStatsImpl().incrementGets(Clock.currentTimeMillis() - getStartTime());
  }
}
