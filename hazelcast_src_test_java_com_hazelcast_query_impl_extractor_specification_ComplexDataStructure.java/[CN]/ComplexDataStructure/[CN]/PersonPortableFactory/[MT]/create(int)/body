{
  if (PersonPortable.ID == classId)   return new PersonPortable();
 else   if (LimbPortable.ID == classId)   return new LimbPortable();
 else   if (FingerPortable.ID == classId)   return new FingerPortable();
 else   return null;
}
