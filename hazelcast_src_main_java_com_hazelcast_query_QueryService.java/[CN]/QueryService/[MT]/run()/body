{
  while (running) {
    Runnable runnable;
    try {
      runnable=queryQ.poll(1,TimeUnit.SECONDS);
      if (runnable != null) {
        runnable.run();
      }
    }
 catch (    Throwable e) {
      e.printStackTrace();
    }
  }
}
