{
  int size=in.readInt();
  List<Edge> list=new ArrayList<Edge>(size);
  for (int i=0; i < size; i++) {
    Data d=new Data();
    d.readData(in);
    Edge e=(Edge)toObject(d);
    list.add(e);
  }
  return list;
}
