{
  if (active && ttl > 0 && Clock.currentTimeMillis() > creationTime + ttl)   active=false;
  if (active && maxIdleMillis > 0 && Clock.currentTimeMillis() > lastAccessTime + maxIdleMillis)   active=false;
  return active;
}
