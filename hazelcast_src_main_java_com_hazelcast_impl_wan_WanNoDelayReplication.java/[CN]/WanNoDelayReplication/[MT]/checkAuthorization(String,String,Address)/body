{
  Operation authorizationCall=new AuthorizationOperation(groupName,groupPassword);
  Future<Boolean> future=node.nodeService.createSingleInvocation(WanReplicationService.SERVICE_NAME,authorizationCall,-1).setTarget(target).setTryCount(1).build().invoke();
  try {
    return future.get();
  }
 catch (  Exception ignored) {
  }
  return false;
}
