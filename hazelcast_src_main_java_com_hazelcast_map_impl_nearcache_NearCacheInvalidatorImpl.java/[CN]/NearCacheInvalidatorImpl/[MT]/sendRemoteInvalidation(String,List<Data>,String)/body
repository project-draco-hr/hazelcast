{
  if (batchingEnabled) {
    for (    Data key : keys) {
      accumulateOrSendBatchInvalidation(mapName,key);
    }
  }
 else {
    sendInvalidationToServerNearCache(mapName,keys);
    sendInvalidationToClientNearCache(mapName,new BatchNearCacheInvalidation(mapName,keys,callerUuid));
  }
}
