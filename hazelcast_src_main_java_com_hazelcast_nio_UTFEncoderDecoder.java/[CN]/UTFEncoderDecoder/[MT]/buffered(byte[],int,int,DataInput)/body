{
  try {
    buffer[0]=buffer[pos];
    return pos + 1;
  }
 catch (  ArrayIndexOutOfBoundsException e) {
    in.readFully(buffer,1,Math.min(buffer.length - 1,utfLength - pos));
    buffer[0]=buffer[1];
    return 2;
  }
}
