{
  int utfLength=0;
  for (int i=beginIndex; i < endIndex; i++) {
    int c=str.charAt(i);
    if (c <= 0) {
      utfLength+=2;
    }
 else     if (c > 0x007F) {
      utfLength+=3;
    }
 else {
      utfLength++;
    }
  }
  return utfLength;
}
