{
  callId=in.readLong();
  backupCount=in.readInt();
  final boolean isData=in.readBoolean();
  if (isData) {
    Data data=new Data();
    data.readData(in);
    response=data;
  }
 else {
    response=in.readObject();
  }
}
