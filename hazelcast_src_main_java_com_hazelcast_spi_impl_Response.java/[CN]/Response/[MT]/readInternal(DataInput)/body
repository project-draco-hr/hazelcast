{
  final boolean isData=in.readBoolean();
  if (isData) {
    Data data=new Data();
    data.readData(in);
    result=(T)data;
  }
 else {
    result=IOUtil.readObject(in);
  }
  exception=in.readBoolean();
}
