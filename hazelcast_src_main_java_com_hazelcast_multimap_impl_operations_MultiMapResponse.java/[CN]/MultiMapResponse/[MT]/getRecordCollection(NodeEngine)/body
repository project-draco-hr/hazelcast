{
  if (collection == null) {
    return pickEmptyCollection(collectionType);
  }
  final Collection<MultiMapRecord> newCollection=pickAndCreateCollection(collectionType,collection.size());
  for (  Object obj : collection) {
    MultiMapRecord record=nodeEngine.toObject(obj);
    newCollection.add(record);
  }
  return newCollection;
}
