{
  Thread t=new Thread(new Runnable(){
    public void run(){
      try {
        Socket socket=connect0();
        put(socket,key.getBytes(),value.getBytes(),0);
        socket.close();
      }
 catch (      IOException e) {
        e.printStackTrace();
      }
    }
  }
);
  t.start();
  t.join();
}
