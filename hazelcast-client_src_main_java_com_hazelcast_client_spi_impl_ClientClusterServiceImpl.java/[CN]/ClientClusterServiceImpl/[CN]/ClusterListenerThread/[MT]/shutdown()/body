{
  interrupt();
  final Connection c=conn;
  if (c != null) {
    try {
      c.close();
    }
 catch (    IOException e) {
      logger.log(Level.WARNING,"Error while closing connection!",e);
    }
  }
}
