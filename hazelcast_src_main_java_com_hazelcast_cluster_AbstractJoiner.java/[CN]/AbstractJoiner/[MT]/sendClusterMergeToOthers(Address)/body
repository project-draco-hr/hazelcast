{
  for (  MemberImpl member : node.getClusterService().getMemberList()) {
    if (!member.localMember()) {
      node.nodeEngine.getOperationService().createInvocationBuilder(ClusterServiceImpl.SERVICE_NAME,new MergeClustersOperation(targetAddress),member.getAddress()).setTryCount(1).build().invoke();
    }
  }
}
