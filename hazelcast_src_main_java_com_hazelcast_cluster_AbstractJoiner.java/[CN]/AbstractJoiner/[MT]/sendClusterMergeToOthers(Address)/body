{
  for (  MemberImpl member : node.getClusterService().getMemberList()) {
    if (!member.localMember()) {
      node.nodeService.createInvocationBuilder(ClusterService.SERVICE_NAME,new MergeClustersOperation(targetAddress),-1).setTarget(member.getAddress()).setTryCount(1).build().invoke();
    }
  }
}
