{
  for (  MemberImpl member : node.getClusterService().getMemberList()) {
    if (!member.localMember()) {
      node.nodeEngine.getOperationService().createInvocationBuilder(ClusterService.SERVICE_NAME,new MergeClustersOperation(targetAddress),member.getAddress()).setTryCount(1).build().invoke();
    }
  }
}
