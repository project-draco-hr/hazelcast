{
  ClientMessage request=TransactionalMapRemoveParameters.encode(getName(),getTransactionId(),ThreadUtil.getThreadId(),toData(key));
  ClientMessage response=invoke(request);
  return (V)toObject(GenericResultParameters.decode(response).result);
}
