{
  ClientMessage request=TransactionalMapPutParameters.encode(getName(),getTransactionId(),ThreadUtil.getThreadId(),toData(key),toData(value),timeunit.toMillis(ttl));
  ClientMessage response=invoke(request);
  return (V)toObject(GenericResultParameters.decode(response).result);
}
