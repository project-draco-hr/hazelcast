{
  ClientMessage request=TransactionalMapReplaceParameters.encode(getName(),getTransactionId(),ThreadUtil.getThreadId(),toData(key),toData(value));
  ClientMessage response=invoke(request);
  return (V)toObject(GenericResultParameters.decode(response).result);
}
