{
  expectDuplicateElementError("listeners");
  String listenersConfig="   <listeners>" + "        <listener>foo</listener>\n\n" + "   </listeners>\n";
  buildConfig(HAZELCAST_START_TAG + listenersConfig + listenersConfig+ HAZELCAST_END_TAG,null);
}
