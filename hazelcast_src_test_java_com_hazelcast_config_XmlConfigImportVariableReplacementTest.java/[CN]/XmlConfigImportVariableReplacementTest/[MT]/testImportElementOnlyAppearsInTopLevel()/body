{
  String xml=HAZELCAST_START_TAG + "   <network>" + "        <import resource=\"\"/>\n"+ "   </network>"+ HAZELCAST_END_TAG;
  expectInvalid("<import> element can appear only in the top level of the XML");
  buildConfig(xml,null);
}
