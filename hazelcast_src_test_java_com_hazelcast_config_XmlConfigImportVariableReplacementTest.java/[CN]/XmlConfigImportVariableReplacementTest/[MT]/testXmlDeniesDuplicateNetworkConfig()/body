{
  expectDuplicateElementError("network");
  final String networkConfig="    <network>\n" + "        <join>\n" + "            <tcp-ip enabled=\"true\"/>\n"+ "        </join>\n"+ "    </network>\n";
  buildConfig(HAZELCAST_START_TAG + networkConfig + networkConfig+ "</hazelcast>",null);
}
