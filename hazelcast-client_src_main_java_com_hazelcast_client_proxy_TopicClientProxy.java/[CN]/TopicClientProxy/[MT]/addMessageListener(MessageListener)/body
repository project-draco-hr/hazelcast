{
  Protocol request=proxyHelper.createProtocol(Command.TLISTEN,new String[]{getName()},null);
  ListenerThread thread=proxyHelper.createAListenerThread("hz.client.topicListener.",client,request,new MessageLRH(messageListener,this));
  listenerMap.put(messageListener,thread);
  thread.start();
}
