{
  dos.write(response.array());
  if (buffers != null && buffers.length > 0) {
    for (    Data buffer : buffers) {
      if (buffer != null)       buffer.writeData(dos);
    }
    dos.write("\r\n".getBytes());
  }
  return true;
}
