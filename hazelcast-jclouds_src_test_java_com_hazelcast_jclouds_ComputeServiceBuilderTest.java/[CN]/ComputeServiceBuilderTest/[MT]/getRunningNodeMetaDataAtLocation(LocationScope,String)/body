{
  Location location=new LocationBuilder().scope(scope).id(id).description("desc").build();
  return new NodeMetadataBuilder().location(location).id(UuidUtil.newSecureUuidString()).status(NodeMetadata.Status.RUNNING).build();
}
