{
  if (!performanceLogHint) {
    return;
  }
  performanceLogHint=false;
  logger.info(String.format("The HealthMonitor has detected a high load on the system. For more detailed information,%s" + "enable the PerformanceMonitor by adding the property -D%s=true",getLineSeperator(),GroupProperty.PERFORMANCE_MONITOR_ENABLED));
}
