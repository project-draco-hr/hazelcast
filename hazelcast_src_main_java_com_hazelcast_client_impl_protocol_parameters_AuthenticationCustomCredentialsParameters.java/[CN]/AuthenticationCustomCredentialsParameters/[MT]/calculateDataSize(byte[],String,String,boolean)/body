{
  return ClientMessage.HEADER_SIZE + ParameterUtil.calculateByteArrayDataSize(credentials) + ParameterUtil.calculateStringDataSize(uuid)+ ParameterUtil.calculateStringDataSize(ownerUuid)+ Bits.BYTE_SIZE_IN_BYTES;
}
