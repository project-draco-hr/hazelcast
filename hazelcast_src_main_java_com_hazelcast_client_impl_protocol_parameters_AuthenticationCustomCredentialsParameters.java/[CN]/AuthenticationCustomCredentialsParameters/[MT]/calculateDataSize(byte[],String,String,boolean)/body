{
  return ClientMessage.HEADER_SIZE + ParameterUtil.calculateByteArrayDataSize(credentials) + ParameterUtil.calculateStringDataSize(uuid)+ ParameterUtil.calculateStringDataSize(ownerUuid)+ BitUtil.SIZE_OF_BYTE;
}
