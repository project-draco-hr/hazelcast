{
  int total=0;
  total+=4;
  if (cd != null) {
    total+=4;
    total+=4;
    total+=4;
    total+=cd.getBinary().length;
  }
 else {
    total+=4;
  }
  total+=4;
  total+=bufferSize();
  total+=4;
  return total;
}
