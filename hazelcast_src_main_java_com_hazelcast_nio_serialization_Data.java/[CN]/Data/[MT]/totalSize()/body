{
  int total=0;
  total+=4;
  if (classDefinition != null) {
    total+=4;
    total+=4;
    total+=4;
    total+=((BinaryClassDefinition)classDefinition).getBinary().length;
  }
 else {
    total+=4;
  }
  total+=4;
  total+=bufferSize();
  total+=4;
  return total;
}
