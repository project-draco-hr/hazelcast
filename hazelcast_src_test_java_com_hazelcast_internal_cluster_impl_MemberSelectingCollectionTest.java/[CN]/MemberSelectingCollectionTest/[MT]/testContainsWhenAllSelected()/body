{
  final MemberSelectingCollection<MemberImpl> collection=new MemberSelectingCollection<MemberImpl>(members,NO_OP_MEMBER_SELECTOR);
  assertTrue(collection.contains(liteMember));
  assertTrue(collection.contains(thisMember));
  assertTrue(collection.contains(dataMember));
}
