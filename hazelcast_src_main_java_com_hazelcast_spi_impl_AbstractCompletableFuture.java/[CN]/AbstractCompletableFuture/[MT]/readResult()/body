{
  Object result=this.result;
  if (result == NULL_VALUE) {
    try {
      result=readResult();
    }
 catch (    Throwable t) {
      result=t;
    }
    if (resultUpdater.compareAndSet(this,NULL_VALUE,result)) {
      return result;
    }
  }
  return this.result;
}
