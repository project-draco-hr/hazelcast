{
  final Data keyData=getSerializationService().toData(key);
  MultiMapLockRequest request=new MultiMapLockRequest(keyData,ThreadUtil.getThreadId(),Long.MAX_VALUE,getTimeInMillis(time,timeunit),proxyId);
  Boolean result=invoke(request,keyData);
  return result;
}
