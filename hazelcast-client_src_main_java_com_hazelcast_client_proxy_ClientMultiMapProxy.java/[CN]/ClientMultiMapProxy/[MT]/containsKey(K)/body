{
  ValidationUtil.checkIfNull(key,NULL_KEY_IS_NOT_ALLOWED);
  Data keyData=toData(key);
  ContainsEntryRequest request=new ContainsEntryRequest(name,keyData,null);
  Boolean result=invoke(request,keyData);
  return result;
}
