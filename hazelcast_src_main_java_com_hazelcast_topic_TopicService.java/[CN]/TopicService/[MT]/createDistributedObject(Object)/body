{
  final String name=String.valueOf(objectId);
  TopicProxy proxy;
  TopicConfig topicConfig=nodeEngine.getConfig().getTopicConfig(name);
  if (topicConfig.isGlobalOrderingEnabled())   proxy=new TotalOrderedTopicProxy(name,nodeEngine);
 else   proxy=new TopicProxy(name,nodeEngine);
  return proxy;
}
