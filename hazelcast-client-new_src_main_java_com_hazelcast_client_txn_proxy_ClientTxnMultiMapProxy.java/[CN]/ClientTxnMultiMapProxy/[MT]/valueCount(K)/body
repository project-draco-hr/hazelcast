{
  ClientMessage request=TransactionalMultiMapValueCountParameters.encode(getName(),getTransactionId(),ThreadUtil.getThreadId(),toData(key));
  ClientMessage response=invoke(request);
  return IntResultParameters.decode(response).result;
}
