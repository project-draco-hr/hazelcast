{
  try {
    boolean isNull=input.readBoolean();
    if (isNull) {
      return null;
    }
    return new HeapData(input.readByteArray());
  }
 catch (  Throwable e) {
    throw handleException(e);
  }
}
