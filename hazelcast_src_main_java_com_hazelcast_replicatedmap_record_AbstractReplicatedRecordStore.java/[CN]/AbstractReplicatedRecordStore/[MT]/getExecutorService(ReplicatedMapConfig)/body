{
  ExecutorService es=replicatedMapConfig.getReplicatorExecutorService();
  if (es != null) {
    return new WrappedExecutorService(es);
  }
  return Executors.newSingleThreadScheduledExecutor(new NamedThreadFactory(ReplicatedMapService.SERVICE_NAME + "." + name+ ".replicator"));
}
