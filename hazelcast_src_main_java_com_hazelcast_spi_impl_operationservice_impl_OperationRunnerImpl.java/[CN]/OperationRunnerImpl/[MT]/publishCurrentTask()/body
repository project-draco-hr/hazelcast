{
  boolean isClientRunnable=currentTask instanceof MessageTask || currentTask instanceof ClientEngineImpl.ClientPacketProcessor;
  return (getPartitionId() != AD_HOC_PARTITION_ID && (currentTask == null || isClientRunnable));
}
