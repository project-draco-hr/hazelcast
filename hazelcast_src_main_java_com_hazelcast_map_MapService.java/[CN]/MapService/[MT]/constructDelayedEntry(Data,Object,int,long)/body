{
  final long now=System.nanoTime();
  final long nanoWriteDelay=TimeUnit.MILLISECONDS.toNanos(writeDelayMillis);
  final DelayedEntry<Data,Object> delayedEntry=DelayedEntry.create(key,value,now + nanoWriteDelay,partitionId);
  return delayedEntry;
}
