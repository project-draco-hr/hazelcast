{
  if (getMapContainer(mapName).getIdleEvictionScheduler() == null)   getMapContainer(mapName).createIdleEvictionScheduler(nodeEngine);
  getMapContainer(mapName).getIdleEvictionScheduler().schedule(delay,key,null);
}
