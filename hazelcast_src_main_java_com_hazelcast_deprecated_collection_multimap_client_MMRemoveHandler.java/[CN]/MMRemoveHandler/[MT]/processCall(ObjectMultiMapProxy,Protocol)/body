{
  Data key=protocol.buffers[0];
  if (protocol.buffers.length > 1) {
    Data value=protocol.buffers[1];
    boolean removed=proxy.remove(key,value);
    return protocol.success(String.valueOf(removed));
  }
  Collection<Object> result=proxy.remove(key);
  return success(proxy,protocol,result);
}
