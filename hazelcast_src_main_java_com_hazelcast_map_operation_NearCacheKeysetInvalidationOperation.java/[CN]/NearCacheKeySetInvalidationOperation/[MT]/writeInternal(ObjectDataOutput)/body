{
  super.writeInternal(out);
  out.writeUTF(mapName);
  if (keys == null) {
    out.writeInt(-1);
  }
 else {
    out.writeInt(keys.size());
    for (    Data key : keys) {
      key.writeData(out);
    }
  }
}
