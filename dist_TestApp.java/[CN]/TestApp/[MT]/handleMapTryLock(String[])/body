{
  String key=args[1];
  long time=(args.length > 2) ? Long.valueOf(args[2]) : 0;
  boolean locked=false;
  if (time == 0)   locked=((IMap)map).tryLock(key);
 else   locked=((IMap)map).tryLock(key,time,TimeUnit.SECONDS);
  print(locked);
}
