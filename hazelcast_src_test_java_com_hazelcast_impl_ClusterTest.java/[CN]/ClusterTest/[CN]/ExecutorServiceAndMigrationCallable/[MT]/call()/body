{
  IMap<Object,Object> map=Hazelcast.getMap("map1");
  map.lock("T1");
  try {
    ISet<Object> set=Hazelcast.getSet("setName1");
    set.size();
    set.add(i);
    if (i % 10 == 0) {
      set.remove(i);
    }
  }
  finally {
    map.unlock("T1");
  }
  return i;
}
