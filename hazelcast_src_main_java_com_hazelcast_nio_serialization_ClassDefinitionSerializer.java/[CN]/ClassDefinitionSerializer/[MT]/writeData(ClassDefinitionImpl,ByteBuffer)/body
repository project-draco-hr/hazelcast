{
  if (isStatusSet(ST_DATA)) {
    return true;
  }
  if (buffer == null) {
    buffer=ByteBuffer.wrap(cd.getBinary());
  }
  if (!flushBuffer(destination)) {
    return false;
  }
  buffer=null;
  setStatus(ST_DATA);
  return true;
}
