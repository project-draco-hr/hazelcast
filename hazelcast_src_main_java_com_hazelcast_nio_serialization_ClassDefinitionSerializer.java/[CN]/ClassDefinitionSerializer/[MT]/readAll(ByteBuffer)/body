{
  for (; classDefIndex < classDefCount; classDefIndex++) {
    if (!readHeader(source)) {
      return false;
    }
    if (!readData(source)) {
      return false;
    }
    clearStatus(ST_HEADER);
    clearStatus(ST_DATA);
  }
  return true;
}
