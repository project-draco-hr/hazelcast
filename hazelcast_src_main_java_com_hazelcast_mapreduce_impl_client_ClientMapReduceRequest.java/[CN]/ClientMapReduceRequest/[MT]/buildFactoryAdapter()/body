{
  return new MapReduceOperationFactory(){
    @Override public Operation createOperation(    int partitionId,    List keys){
      return new KeyValueMapReduceOperation(name,jobId,chunkSize,keys,predicate,mapper,keyValueSource,combinerFactory);
    }
  }
;
}
