{
  int size=100;
  for (int i=0; i < size; i++) {
    map.put(i,i);
  }
  c.send(new MapKeySetRequest(mapName));
  Set keyset=(Set)c.receive();
  for (  Object o : keyset) {
    System.out.println(o);
  }
}
