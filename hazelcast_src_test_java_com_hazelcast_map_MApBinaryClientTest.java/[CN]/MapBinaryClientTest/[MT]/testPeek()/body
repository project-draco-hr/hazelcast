{
  IQueue q=hz.getQueue(mapName);
  c.send(new PeekRequest(mapName));
  Object result=c.receive();
  assertNull(result);
  q.offer("item1");
  c.send(new PeekRequest(mapName));
  result=c.receive();
  assertEquals("item1",result);
  assertEquals(1,q.size());
}
