{
  if (!f.exists()) {
    return;
  }
  final File[] subFiles=f.listFiles();
  if (subFiles != null) {
    for (    File sf : subFiles) {
      delete(sf);
    }
  }
  if (!f.delete()) {
    throw new HazelcastException("Failed to delete " + f);
  }
}
