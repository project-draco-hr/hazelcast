{
  return executorService.submit(new Callable<T>(){
    public T call() throws Exception {
      Data dKey=proxyHelper.toData(key);
      return (T)proxyHelper.doCommandAsObject(dKey,Command.EXECUTE,new String[]{getName()},proxyHelper.toData(task),dKey);
    }
  }
);
}
