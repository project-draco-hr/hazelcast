{
  return executorService.submit(new Callable<T>(){
    public T call() throws Exception {
      return (T)proxyHelper.doCommandAsObject(proxyHelper.toData(key),Command.EXECUTE,new String[]{},proxyHelper.toData(task),proxyHelper.toData(key));
    }
  }
);
}
