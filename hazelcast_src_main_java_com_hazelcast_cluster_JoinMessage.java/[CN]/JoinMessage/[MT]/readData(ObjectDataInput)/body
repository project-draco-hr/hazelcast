{
  packetVersion=in.readByte();
  buildNumber=in.readInt();
  address=new Address();
  address.readData(in);
  uuid=in.readUTF();
  config=new Config();
  config.readData(in);
  memberCount=in.readInt();
}
