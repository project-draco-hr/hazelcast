{
  Hazelcast.shutdownAll();
  this.hz1=this.hz2=null;
  if (this.multicastGroup == null)   System.clearProperty("hazelcast.multicast.group");
 else   System.setProperty("hazelcast.multicast.group",this.multicastGroup);
}
