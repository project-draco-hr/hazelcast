{
  TestUtil.terminateInstance(hz);
  hz.shutdown();
  boolean hasMember=true;
  do {
    try {
      Member m=hz.getCluster().getLocalMember();
      hasMember=m != null;
    }
 catch (    Throwable _) {
      hasMember=false;
    }
  }
 while (hasMember);
}
