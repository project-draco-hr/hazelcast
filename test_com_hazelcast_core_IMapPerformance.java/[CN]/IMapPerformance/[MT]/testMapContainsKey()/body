{
  String test="testMapContainsKey";
  for (int i=0; i < ops; ++i) {
    map.put(i,"Hello World");
  }
  t=new PerformanceTimer(test,ops);
  for (int i=0; i < ops; ++i) {
    map.containsKey(i);
  }
}
