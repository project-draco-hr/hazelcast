{
  DefaultContext<K,V> context=new DefaultContext<K,V>(configuration.getCombinerFactory(),partitionId,mapCombineTask);
  if (this.context.compareAndSet(null,context)) {
    return context;
  }
  return this.context.get();
}
