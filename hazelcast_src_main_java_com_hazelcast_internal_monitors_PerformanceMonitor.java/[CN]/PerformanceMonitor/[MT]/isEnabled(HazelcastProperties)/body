{
  String s=properties.getString(ENABLED);
  if (s != null) {
    return properties.getBoolean(ENABLED);
  }
  s=properties.get("hazelcast.performance.monitoring.enabled");
  if (s != null) {
    logger.warning("Don't use deprecated 'hazelcast.performance.monitoring.enabled' " + "but use '" + ENABLED.getName() + "' instead. "+ "The former name will be removed in Hazelcast 3.8.");
  }
  return Boolean.parseBoolean(s);
}
