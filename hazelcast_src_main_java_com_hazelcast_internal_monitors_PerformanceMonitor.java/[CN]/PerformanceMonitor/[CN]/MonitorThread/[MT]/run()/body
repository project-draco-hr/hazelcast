{
  try {
    while (node.getState() == NodeState.ACTIVE) {
      performanceLogFile.render();
      sleep();
    }
    performanceLogFile.render();
  }
 catch (  Throwable t) {
    logger.warning(t.getMessage(),t);
  }
}
