{
  ClientMessage request=LockIsLockedCodec.encodeRequest(getName());
  LockIsLockedCodec.ResponseParameters resultParameters=LockIsLockedCodec.decodeResponse((ClientMessage)invoke(request));
  return resultParameters.response;
}
