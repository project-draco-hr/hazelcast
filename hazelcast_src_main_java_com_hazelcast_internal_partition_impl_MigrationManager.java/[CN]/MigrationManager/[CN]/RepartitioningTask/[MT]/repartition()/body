{
  if (!isAllowed()) {
    return null;
  }
  Address[][] newState=partitionStateManager.repartition();
  if (newState == null) {
    return null;
  }
  if (!isAllowed()) {
    return null;
  }
  return newState;
}
