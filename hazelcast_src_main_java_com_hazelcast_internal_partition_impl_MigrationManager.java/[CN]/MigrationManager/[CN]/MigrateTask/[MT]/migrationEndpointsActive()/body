{
  final ClusterServiceImpl clusterService=node.getClusterService();
  return clusterService.getMember(migrationInfo.getSource()) != null && clusterService.getMember(migrationInfo.getDestination()) != null;
}
