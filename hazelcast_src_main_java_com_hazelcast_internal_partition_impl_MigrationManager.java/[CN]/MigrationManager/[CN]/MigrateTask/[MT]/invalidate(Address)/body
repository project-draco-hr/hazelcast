{
  if (migrationInfo.isValid()) {
    boolean valid=!(reason == MigrateTaskReason.REPARTITIONING || migrationInfo.getSource().equals(address) || migrationInfo.getDestination().equals(address));
    if (!valid) {
      migrationInfo.setStatus(MigrationStatus.INVALID);
    }
  }
}
