{
  live=false;
  for (  Connection conn : mapConnections.values()) {
    try {
      remove(conn);
    }
 catch (    Exception e) {
    }
  }
  InSelector.get().shutdown();
  OutSelector.get().shutdown();
}
