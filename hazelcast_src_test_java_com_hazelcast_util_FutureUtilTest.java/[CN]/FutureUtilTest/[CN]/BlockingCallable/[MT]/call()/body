{
  if (waitLock.compareAndSet(true,false)) {
    try {
      for (; ; ) {
        Thread.sleep(100);
      }
    }
 catch (    InterruptedException ignored) {
      interrupted.set(true);
    }
  }
  return 1;
}
