{
  lock.lock();
  try {
    final Worker worker=new Worker();
    final Thread thread=threadFactory.newThread(worker);
    if (start) {
      thread.start();
    }
    activeThreadCount++;
    threads.add(thread);
  }
  finally {
    lock.unlock();
  }
}
