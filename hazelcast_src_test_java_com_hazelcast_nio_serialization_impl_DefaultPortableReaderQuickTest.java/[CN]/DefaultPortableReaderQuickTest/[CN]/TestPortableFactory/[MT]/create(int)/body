{
  if (CarPortable.ID == classId)   return new CarPortable();
 else   if (EnginePortable.ID == classId)   return new EnginePortable();
 else   if (WheelPortable.ID == classId)   return new WheelPortable();
 else   if (ChipPortable.ID == classId)   return new ChipPortable();
 else   return null;
}
