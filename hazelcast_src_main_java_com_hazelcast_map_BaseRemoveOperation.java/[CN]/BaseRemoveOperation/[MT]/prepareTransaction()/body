{
  if (TRANSACTION_ENABLED) {
    if (txnId != null) {
      pc.addTransactionLogItem(txnId,new TransactionLogItem(name,dataKey,null,false,true));
      if (RETURN_RESPONSE)       responseHandler.sendResponse(null);
      return true;
    }
  }
  return false;
}
