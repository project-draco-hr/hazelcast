{
  if (mapType == MAP_TYPE_MAP)   return InstanceType.MAP;
 else   if (mapType == MAP_TYPE_SET)   return InstanceType.SET;
 else   if (mapType == MAP_TYPE_LIST)   return InstanceType.LIST;
 else   throw new RuntimeException("Unknown MProxy type " + mapType);
}
