{
  if (qProxy == null) {
    ListenerManager.get().addListener(name,listener,null,true,ListenerManager.LISTENER_TYPE_MESSAGE);
  }
 else {
    ListenerManager.get().addListener(qProxy.name,listener,null,true,ListenerManager.LISTENER_TYPE_MESSAGE);
  }
}
