{
  if (mapType == MAP_TYPE_MAP)   return ICommon.InstanceType.MAP;
 else   if (mapType == MAP_TYPE_SET)   return ICommon.InstanceType.SET;
 else   if (mapType == MAP_TYPE_LIST)   return ICommon.InstanceType.LIST;
 else   throw new RuntimeException("Unknown MProxy type " + mapType);
}
