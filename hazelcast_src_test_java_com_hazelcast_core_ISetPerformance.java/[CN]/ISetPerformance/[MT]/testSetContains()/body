{
  String test="testSetContains";
  for (int i=0; i < ops; ++i) {
    set.add("Hello" + i);
  }
  t=new PerformanceTimer(test,ops);
  for (int i=0; i < ops; ++i) {
    set.contains("Hello" + i);
  }
}
