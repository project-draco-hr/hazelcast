{
  if (!active) {
    return null;
  }
  E e=queue.poll();
  if (e == null) {
    try {
      e=factory.create();
    }
 catch (    Exception ex) {
      throw new HazelcastException(ex);
    }
  }
  return e;
}
