{
  if (!(response instanceof Operation)) {
    response=new Response(response);
  }
  boolean nb=(response instanceof NonBlockingOperation);
  nodeService.getNode().clusterImpl.send(new SimpleSocketWritable(REMOTE_CALL,serviceName,toData(response),callId,partitionId,replicaIndex,null,nb),conn);
}
