{
  if (obj instanceof Throwable) {
    nodeService.getNode().getLogger(getClass().getName()).log(Level.WARNING,"Error while executing operation: " + operation,(Throwable)obj);
  }
 else {
    throw new IllegalStateException("Can not send response for NoReply operation: " + operation);
  }
}
