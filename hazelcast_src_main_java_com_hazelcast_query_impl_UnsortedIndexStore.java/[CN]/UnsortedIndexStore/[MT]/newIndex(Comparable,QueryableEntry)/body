{
  final Object indexKey=record.getIndexKey();
  final ConcurrentMap<Object,QueryableEntry> records=ConcurrencyUtil.getOrPutIfAbsent(mapRecords,newValue,recordsMapConstructor);
  records.put(indexKey,record);
}
