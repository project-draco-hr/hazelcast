{
  TypeConverters.TypeConverter converter=new TypeConverters.TypeConverter(){
    @Override Comparable convertInternal(    Comparable value){
      return value;
    }
  }
;
  assertEquals(IndexImpl.NULL,converter.convert(null));
}
