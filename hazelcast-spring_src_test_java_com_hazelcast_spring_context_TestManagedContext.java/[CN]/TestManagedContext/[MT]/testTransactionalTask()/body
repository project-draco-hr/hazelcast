{
  Future<Void> f=instance1.getExecutorService().submit(new SomeTransactionalTask());
  f.get();
  Assert.assertTrue("transaction manager could not proxy the submitted task.",transactionManager.isUsed());
}
