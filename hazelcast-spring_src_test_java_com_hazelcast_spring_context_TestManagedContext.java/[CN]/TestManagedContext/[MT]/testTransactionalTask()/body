{
  Future f=instance1.getExecutorService("test").submit(new DistributedTask(new SomeTransactionalTask(),instance2.getCluster().getLocalMember()));
  f.get();
  Assert.assertTrue("transaction manager could not proxy the submitted task.",transactionManager.isCommitted());
}
