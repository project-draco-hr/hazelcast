{
synchronized (indexSyncLock) {
    while (indexLocked) {
      try {
        indexWriteLock.wait();
      }
 catch (      InterruptedException e) {
        e.printStackTrace();
      }
    }
    readerCount.incrementAndGet();
  }
}
