{
synchronized (indexSyncLock) {
    indexLocked=true;
    while (readerCount.get() > 0) {
      try {
        indexReadLock.wait();
      }
 catch (      InterruptedException e) {
        e.printStackTrace();
      }
    }
  }
}
