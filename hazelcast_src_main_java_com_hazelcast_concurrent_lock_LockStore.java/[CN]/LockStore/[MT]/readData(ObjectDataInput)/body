{
  namespace=in.readObject();
  backupCount=in.readInt();
  asyncBackupCount=in.readInt();
  int len=in.readInt();
  if (len > 0) {
    for (int i=0; i < len; i++) {
      Data key=new Data();
      key.readData(in);
      LockInfo lock=new LockInfo();
      lock.readData(in);
      locks.put(key,lock);
    }
  }
}
