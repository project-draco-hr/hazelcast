{
  int size=in.readInt();
  Collection coll=new ArrayList(size);
  if (size > 0) {
    for (int i=0; i < size; i++) {
      Object value=in.readObject();
      coll.add(value);
    }
  }
  return coll;
}
