{
  if (valid) {
    if (isExpired()) {
      queue.remove(this);
      waitSupport.onWaitExpire();
    }
 else     if (isCancelled()) {
      queue.remove(this);
      op.getResponseHandler().sendResponse(error);
    }
  }
}
