{
  if (slow) {
    try {
      Thread.sleep(2000);
    }
 catch (    InterruptedException e) {
      e.printStackTrace();
    }
  }
  Map result=new HashMap();
  for (  Object key : keys) {
    result.put(key,key);
  }
  return result;
}
