{
  String name=randomMapName();
  Config config=getConfig();
  config.addMapConfig(new MapConfig(name).setOptimizeQueries(true).addMapIndexConfig(new MapIndexConfig("timestamp",true)));
  testQueryUsingPortableObject(config,name);
}
