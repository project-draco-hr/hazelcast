{
  super(coreThreadSize,maxThreadSize,queueCapacity,backlogIntervalInMillis,namePrefix,threadFactory,keepAliveMillis,allowCoreThreadTimeout);
  this.queue=new LinkedBlockingQueue<WorkerTask>(queueCapacity > 0 ? queueCapacity : Integer.MAX_VALUE);
}
