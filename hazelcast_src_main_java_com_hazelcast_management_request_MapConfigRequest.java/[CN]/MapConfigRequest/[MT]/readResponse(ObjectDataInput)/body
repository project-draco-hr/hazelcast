{
  update=in.readBoolean();
  if (!update) {
    if (in.readBoolean()) {
      MapConfig cfg=new MapConfig();
      cfg.readData(in);
      return cfg;
    }
 else {
      return null;
    }
  }
  return in.readUTF();
}
