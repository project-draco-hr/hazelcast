{
  final DistributedLock lock=locks.get(key);
  if (lock == null)   return false;
 else {
    if (lock.isEvictable()) {
      locks.remove(key);
    }
 else {
      lock.clear();
    }
    return true;
  }
}
