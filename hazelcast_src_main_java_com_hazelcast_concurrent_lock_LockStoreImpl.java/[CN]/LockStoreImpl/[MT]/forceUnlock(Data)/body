{
  final LockInfo lock=locks.get(key);
  if (lock == null)   return false;
 else {
    if (lock.isEvictable()) {
      locks.remove(key);
      lock.cancelEviction();
    }
 else {
      lock.clear();
    }
    return true;
  }
}
