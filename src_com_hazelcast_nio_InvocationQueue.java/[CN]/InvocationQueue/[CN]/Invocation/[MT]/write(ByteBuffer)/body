{
  socketBB.put(bbSizes.array(),0,bbSizes.limit());
  socketBB.put(bbHeader.array(),0,bbHeader.limit());
  if (key.size() > 0)   key.copyToBuffer(socketBB);
  if (value.size() > 0)   value.copyToBuffer(socketBB);
}
