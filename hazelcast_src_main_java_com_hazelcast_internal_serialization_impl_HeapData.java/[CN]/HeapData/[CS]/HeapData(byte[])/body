{
  if (payload != null && payload.length > 0 && payload.length < DATA_OFFSET) {
    throw new IllegalArgumentException("Data should be either empty or should contain more than " + HeapData.DATA_OFFSET + " bytes! -> "+ Arrays.toString(payload));
  }
  this.payload=payload;
}
