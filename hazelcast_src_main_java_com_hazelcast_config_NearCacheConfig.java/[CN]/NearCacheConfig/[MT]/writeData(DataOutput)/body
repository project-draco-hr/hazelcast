{
  out.writeInt(timeToLiveSeconds);
  out.writeInt(maxSize);
  out.writeBoolean(evictionPolicy == null);
  if (evictionPolicy != null)   out.writeUTF(evictionPolicy);
  out.writeInt(maxIdleSeconds);
  out.writeBoolean(invalidateOnChange);
}
