{
  final ByteBuffer byteBuffer=buffer().byteBuffer();
  if (byteBuffer != null) {
    byteBuffer.limit(index());
    byteBuffer.position(index());
    byteBuffer.flip();
    return byteBuffer;
  }
  return null;
}
