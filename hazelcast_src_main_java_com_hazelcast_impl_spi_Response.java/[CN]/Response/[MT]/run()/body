{
  if (opBeforeData != null) {
    Operation op=(Operation)toObject(opBeforeData);
    op.setCallId(getCallId()).setService(getService()).setPartitionId(getPartitionId()).setCaller(getCaller()).setCallId(getCallId()).setNodeService(getNodeService());
    op.run();
  }
  long callId=getCallId();
  getNodeService().notifyCall(callId,Response.this);
}
