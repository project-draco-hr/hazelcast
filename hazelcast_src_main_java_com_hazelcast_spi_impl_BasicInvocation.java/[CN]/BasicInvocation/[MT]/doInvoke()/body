{
  if (!engineActive()) {
    return;
  }
  invokeCount++;
  if (!initInvocationTarget()) {
    return;
  }
  setInvocationTime(op,nodeEngine.getClusterTime());
  if (remote) {
    doInvokeRemote();
  }
 else {
    doInvokeLocal();
  }
}
