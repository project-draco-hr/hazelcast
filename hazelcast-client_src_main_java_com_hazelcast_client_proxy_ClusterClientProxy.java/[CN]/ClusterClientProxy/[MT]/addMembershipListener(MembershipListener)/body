{
  Protocol request=proxyHelper.createProtocol(Command.MEMBERLISTEN,null,null);
  ListenerThread thread=proxyHelper.createAListenerThread("hz.client.membershipListener.",client,request,new MembershipLRH(listener,this));
  listenerMap.put(listener,thread);
  thread.start();
}
