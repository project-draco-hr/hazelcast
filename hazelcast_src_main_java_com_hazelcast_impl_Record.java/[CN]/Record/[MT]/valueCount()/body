{
  int count=0;
  if (getValueData() != null) {
    count=1;
  }
 else   if (getMultiValues() != null) {
    count=getMultiValues().size();
  }
 else   if (copyCount > 0) {
    count+=copyCount;
  }
  return count;
}
