{
  int count=0;
  if (getValue() != null) {
    count=1;
  }
 else   if (getLsValues() != null) {
    count=getLsValues().size();
  }
 else   if (getCopyCount() > 0) {
    count+=getCopyCount();
  }
  return count;
}
