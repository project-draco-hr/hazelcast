{
  int count=0;
  if (getValue() != null) {
    count=1;
  }
 else   if (getMultiValues() != null) {
    count=getMultiValues().size();
  }
 else   if (getCopyCount() > 0) {
    count+=getCopyCount();
  }
  return count;
}
