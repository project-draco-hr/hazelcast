{
  long cost=0;
  if (getValue() != null) {
    cost=getValue().size();
    if (getCopyCount() > 0) {
      cost*=getCopyCount();
    }
  }
 else   if (getMultiValues() != null) {
    for (    Data data : getMultiValues()) {
      cost+=data.size();
    }
  }
  return cost + getKey().size();
}
