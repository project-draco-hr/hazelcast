{
  long cost=0;
  if (getValue() != null) {
    cost=getValue().size();
    if (getCopyCount() > 0) {
      cost*=getCopyCount();
    }
  }
 else   if (getLsValues() != null) {
    for (    Data data : getLsValues()) {
      cost+=data.size();
    }
  }
  return cost + getKey().size();
}
