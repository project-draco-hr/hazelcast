{
  long cost=0;
  if (getValue() != null) {
    cost=getValue().size();
    if (copyCount > 0) {
      cost*=copyCount;
    }
  }
 else   if (getMultiValues() != null) {
    for (    Data data : getMultiValues()) {
      cost+=data.size();
    }
  }
  return cost + getKey().size() + 250;
}
