{
  if (now) {
    doShutdown(force);
  }
 else {
    new Thread(new Runnable(){
      public void run(){
        ThreadContext.get().setCurrentFactory(factory);
        doShutdown(force);
      }
    }
).start();
  }
}
