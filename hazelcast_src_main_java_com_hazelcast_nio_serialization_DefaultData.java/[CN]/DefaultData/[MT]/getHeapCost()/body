{
  final int arrayHeaderSizeInBytes=16;
  int total=0;
  total+=Bits.INT_SIZE_IN_BYTES;
  if (data != null) {
    total+=arrayHeaderSizeInBytes;
    total+=data.length;
  }
 else {
    total+=Bits.INT_SIZE_IN_BYTES;
  }
  total+=Bits.INT_SIZE_IN_BYTES;
  return total;
}
