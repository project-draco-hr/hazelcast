{
  if (committedHeap != that.committedHeap) {
    return false;
  }
 else   if (maxHeap != that.maxHeap) {
    return false;
  }
 else   if (usedHeap != that.usedHeap) {
    return false;
  }
  return true;
}
