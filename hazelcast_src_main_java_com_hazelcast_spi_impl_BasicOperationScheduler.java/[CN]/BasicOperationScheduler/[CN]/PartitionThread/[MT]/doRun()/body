{
  for (; ; ) {
    Object task;
    try {
      task=workQueue.take();
    }
 catch (    InterruptedException e) {
      continue;
    }
    if (task instanceof PoisonPill) {
      return;
    }
    processPriorityMessages();
    process(task);
  }
}
