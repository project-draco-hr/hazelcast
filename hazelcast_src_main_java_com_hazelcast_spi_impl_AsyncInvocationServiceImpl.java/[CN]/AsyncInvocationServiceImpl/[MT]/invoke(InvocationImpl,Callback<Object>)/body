{
  try {
    executor.execute(new AsyncInvocation(invocation,responseCallback));
  }
 catch (  RejectedExecutionException e) {
    logger.log(Level.WARNING,"Capacity overloaded! Executing " + invocation.getOperation() + " in current thread, instead of invoking async.");
    invokeInCurrentThread(invocation,responseCallback);
  }
}
