{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  ClientRegistrationKey that=(ClientRegistrationKey)o;
  return !(userRegistrationId != null ? !userRegistrationId.equals(that.userRegistrationId) : that.userRegistrationId != null);
}
