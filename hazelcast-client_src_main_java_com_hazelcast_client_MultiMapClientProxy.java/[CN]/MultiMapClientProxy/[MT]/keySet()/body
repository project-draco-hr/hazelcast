{
  Protocol protocol=proxyHelper.doCommand(Command.MMKEYS,new String[]{getName()},null);
  Set set=new HashSet();
  if (protocol.hasBuffer()) {
    for (    ByteBuffer bb : protocol.buffers) {
      set.add(client.getSerializationService().toObject(new Data(SerializationConstants.CONSTANT_TYPE_BYTE_ARRAY,bb.array())));
    }
  }
  return set;
}
