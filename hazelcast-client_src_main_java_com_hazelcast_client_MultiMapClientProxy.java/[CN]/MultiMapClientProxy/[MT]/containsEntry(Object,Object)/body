{
  check(key);
  check(value);
  return protocolProxyHelper.doCommandAsBoolean(Command.MMCONTAINSENTRY,new String[]{getName()},client.getSerializationService().toData(key),client.getSerializationService().toData(value));
}
