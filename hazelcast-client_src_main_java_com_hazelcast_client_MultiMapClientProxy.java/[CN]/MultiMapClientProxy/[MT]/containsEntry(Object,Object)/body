{
  check(key);
  check(value);
  return proxyHelper.doCommandAsBoolean(Command.MMCONTAINSENTRY,new String[]{getName()},client.getSerializationService().toData(key),client.getSerializationService().toData(value));
}
