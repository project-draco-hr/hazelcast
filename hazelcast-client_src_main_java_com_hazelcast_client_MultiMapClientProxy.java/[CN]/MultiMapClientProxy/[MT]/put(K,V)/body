{
  check(key);
  check(value);
  return protocolProxyHelper.doCommandAsBoolean(Command.MMPUT,new String[]{getName()},client.getSerializationService().toData(key),client.getSerializationService().toData(value));
}
