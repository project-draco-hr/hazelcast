{
  final NodeEngine nodeEngine=getNodeEngine();
  try {
    nodeEngine.getOperationService().invokeOnAllPartitions(SERVICE_NAME,new LoadKeysOperationFactory(name));
  }
 catch (  Throwable t) {
    throw ExceptionUtil.rethrow(t);
  }
}
