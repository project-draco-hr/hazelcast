{
  int len=lsData.size();
  for (int i=0; i < len; i++) {
    ByteBuffer bb=lsData.get(i);
    bb.clear();
    ThreadContext.get().getBufferPool().release(bb);
  }
  lsData.clear();
  prepareForRead();
}
