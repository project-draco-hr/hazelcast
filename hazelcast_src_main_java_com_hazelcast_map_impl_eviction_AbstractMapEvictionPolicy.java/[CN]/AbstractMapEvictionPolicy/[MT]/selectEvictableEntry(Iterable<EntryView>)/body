{
  EntryView selected=null;
  for (  EntryView candidate : samples) {
    if (selected == null) {
      selected=candidate;
    }
 else     if (compare(selected,candidate)) {
      selected=candidate;
    }
  }
  return selected;
}
