{
  if (sk.isValid() && sk.isReadable()) {
    readEvents.inc();
    SelectionHandler handler=(SelectionHandler)sk.attachment();
    try {
      handler.handle();
    }
 catch (    Throwable t) {
      handler.onFailure(t);
    }
  }
}
