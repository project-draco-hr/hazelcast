{
  try {
    start.await();
    while (isRunning.get()) {
      instance.shutdown();
      Thread.sleep(10l);
      instance=newInstance();
    }
  }
 catch (  InterruptedException e) {
    e.printStackTrace();
  }
}
