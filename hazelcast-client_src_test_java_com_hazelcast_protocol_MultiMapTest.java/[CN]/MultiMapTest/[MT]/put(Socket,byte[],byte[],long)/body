{
  OutputStream out=doOp("MMPUT default " + ttl + " #2","" + key.length + " "+ value.length,socket);
  out.write(key);
  out.write(value);
  out.write("\r\n".getBytes());
  out.flush();
  return read(socket);
}
