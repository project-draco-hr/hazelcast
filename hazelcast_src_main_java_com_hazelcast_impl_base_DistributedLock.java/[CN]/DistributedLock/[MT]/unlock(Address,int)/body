{
  if (lockCount == 0) {
    return false;
  }
 else {
    if (isLockedBy(address,threadId)) {
      lockCount--;
      if (lockCount == 0) {
        lockAddress=null;
        lockThreadId=-1;
        lockAcquireTime=-1L;
      }
      return true;
    }
  }
  return false;
}
