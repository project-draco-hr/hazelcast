{
  if (DEBUG) {
    System.out.println(Thread.currentThread().getName() + " Closing Socket. cause:  " + e);
  }
  if (DEBUG) {
    e.printStackTrace();
  }
  if (sk != null)   sk.cancel();
  connection.close();
}
