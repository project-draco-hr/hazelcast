{
  if (getPredicate() == null) {
    return new EntryEventFilter(parameters.includeValue,parameters.key.length == 0 ? null : new DefaultData(parameters.key));
  }
  return new QueryEventFilter(parameters.includeValue,parameters.key.length == 0 ? null : new DefaultData(parameters.key),getPredicate());
}
