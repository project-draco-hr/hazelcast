{
  return new TargetDisconnectedException(format("Closing connection to member %s." + " The client has closed the connection to this member, after receiving a member left event from the cluster.",member.getAddress()));
}
