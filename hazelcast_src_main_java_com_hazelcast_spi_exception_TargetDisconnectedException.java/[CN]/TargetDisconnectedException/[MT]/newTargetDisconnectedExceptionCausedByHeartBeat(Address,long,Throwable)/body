{
  return new TargetDisconnectedException(format("Disconnecting from member %s due to heartbeat problems. Current time: %s. Last heartbeat: %s",memberAddress,timeToString(System.currentTimeMillis()),(lastHeartbeatMillis == 0) ? "never" : timeToString(lastHeartbeatMillis)),cause);
}
