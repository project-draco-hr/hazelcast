{
  if (startMigration()) {
    try {
      migrate();
    }
 catch (    Throwable e) {
      success=false;
      getLogger().severe("Error while processing " + migrationInfo,e);
    }
 finally {
      afterMigrate();
    }
  }
 else {
    logMigrationCancelled();
  }
}
