{
  assertMigrationInitiatorIsMaster();
  try {
    doRun();
  }
 catch (  Throwable t) {
    logMigrationFailure(t);
    failureReason=t;
  }
 finally {
    if (!success) {
      onExecutionFailure(failureReason);
    }
  }
}
