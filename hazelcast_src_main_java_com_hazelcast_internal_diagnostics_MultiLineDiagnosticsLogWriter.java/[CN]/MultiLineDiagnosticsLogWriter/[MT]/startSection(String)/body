{
  if (sectionLevel == -1) {
    appendDateTime();
    sb.append(' ');
  }
  if (sectionLevel >= 0) {
    sb.append(INDENTS[sectionLevel]);
  }
  sb.append(name);
  sb.append('[');
  sectionLevel++;
}
