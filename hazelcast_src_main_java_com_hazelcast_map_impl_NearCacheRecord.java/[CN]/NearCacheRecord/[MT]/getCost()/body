{
  if (!(value instanceof Data)) {
    return 0;
  }
  if (!(key instanceof Data)) {
    return 0;
  }
  return ((Data)key).getHeapCost() + ((Data)value).getHeapCost() + 2 * (Long.SIZE / Byte.SIZE) + (Long.SIZE / Byte.SIZE) + 3 * (Integer.SIZE / Byte.SIZE);
}
