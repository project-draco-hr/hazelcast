{
  active=false;
  if (future != null) {
    future.cancel(true);
  }
  final ResponseStream s=lastStream;
  if (s != null) {
    try {
      s.end();
    }
 catch (    IOException ignored) {
    }
  }
}
