{
  Record record=recordStore.getRecord(dataKey);
  if (record.getCreationTime() > removeTime || record.getLastUpdateTime() > removeTime) {
    return;
  }
  recordStore.deleteRecord(dataKey);
  merged=true;
}
