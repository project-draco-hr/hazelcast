{
  if (member.localMember()) {
    return 0;
  }
  Integer distance=distances.get(member);
  if (distance != null) {
    return distance;
  }
  calculateDistances();
  Integer d=distances.get(member);
  return (d == null) ? -1 : d;
}
