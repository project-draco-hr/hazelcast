{
  if (result == null) {
synchronized (this) {
      if (result == null) {
        Packet packet=null;
        if (l < 0) {
          packet=queue.take();
        }
 else {
          packet=queue.poll(l,timeUnit);
        }
        result=handleResult(packet);
      }
    }
  }
  return result;
}
