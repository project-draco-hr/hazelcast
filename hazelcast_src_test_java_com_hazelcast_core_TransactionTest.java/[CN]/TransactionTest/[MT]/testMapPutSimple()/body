{
  TransactionalMap txnMap=newTransactionalMapProxy("testMapPutSimple");
  txnMap.begin();
  txnMap.put("1","value");
  assertEquals(1,txnMap.size());
  txnMap.commit();
  assertEquals(1,txnMap.size());
}
