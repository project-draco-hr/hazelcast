{
  TransactionalList list=newTransactionalListProxy("testListCommitTwoSameItems");
  list.begin();
  list.add("item");
  list.add("item");
  assertEquals(2,list.size());
  list.commit();
  assertEquals(2,list.size());
}
