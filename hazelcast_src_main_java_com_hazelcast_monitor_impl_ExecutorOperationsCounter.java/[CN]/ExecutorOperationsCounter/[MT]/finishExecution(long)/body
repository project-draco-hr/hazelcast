{
  executionEnds.count(elapsed);
  if (elapsed > maxExecutionTime.get())   maxExecutionTime.set(elapsed);
  if (elapsed < minExecutionTime.get())   minExecutionTime.set(elapsed);
  publishSubResult();
}
