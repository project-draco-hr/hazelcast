{
  final int len=in.readInt();
  if (len > 0) {
    int pos=in.position();
    try {
      final byte[] bytes=new byte[len];
      in.readFully(bytes);
      return serializer.read(bytes);
    }
  finally {
      in.position(pos + len);
    }
  }
  return null;
}
