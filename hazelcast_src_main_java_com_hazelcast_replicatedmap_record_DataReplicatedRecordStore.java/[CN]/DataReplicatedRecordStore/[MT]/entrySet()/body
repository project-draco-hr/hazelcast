{
  Set entrySet=new HashSet(storage.size());
  for (  Map.Entry entry : storage.entrySet()) {
    entrySet.add(new AbstractMap.SimpleEntry(nodeEngine.toObject(entry.getKey()),nodeEngine.toObject(entry.getValue())));
  }
  return entrySet;
}
