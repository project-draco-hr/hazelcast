{
  lastHandle=Clock.currentTimeMillis();
  if (!connection.isAlive()) {
    return;
  }
  if (lastPacket == null) {
    lastPacket=poll();
  }
  if (lastPacket == null && buffer.position() == 0) {
    ready=true;
    return;
  }
  writeBuffer();
  ready=false;
  registerWrite();
}
