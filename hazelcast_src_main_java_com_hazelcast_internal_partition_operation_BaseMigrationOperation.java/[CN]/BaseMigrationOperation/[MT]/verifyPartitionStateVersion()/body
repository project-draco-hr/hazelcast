{
  InternalPartitionService partitionService=getService();
  int localPartitionStateVersion=partitionService.getPartitionStateVersion();
  if (partitionStateVersion != localPartitionStateVersion) {
    throw new RetryableHazelcastException("Partition state versions are not matching!" + " Local: " + localPartitionStateVersion + ", Master: "+ partitionStateVersion);
  }
}
