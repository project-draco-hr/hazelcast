{
  SimpleExecution simpleExecution=ExecutorManager.get().mapRemoteExecutions.remove(executionId);
  if (simpleExecution == null)   return false;
  return simpleExecution.cancel(mayInterruptIfRunning);
}
