{
  if (DEBUG) {
    logger.log(Level.INFO,"SimpleExecution is cancelling..");
  }
  if (done || cancelled)   return false;
  if (running && mayInterruptIfRunning)   runningThread.interrupt();
  cancelled=true;
  return true;
}
