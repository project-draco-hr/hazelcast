{
  final InternalOperationService operationService=getOperationService(instances[0]);
  for (int partitionId=0; partitionId < PARTITION_COUNT; partitionId++) {
    assertEquals(Boolean.TRUE,operationService.invokeOnPartition(null,new SampleGetOperation(),partitionId).get(10,TimeUnit.SECONDS));
  }
}
