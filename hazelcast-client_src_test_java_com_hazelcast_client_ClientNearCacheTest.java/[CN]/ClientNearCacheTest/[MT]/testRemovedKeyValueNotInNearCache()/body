{
  final IMap map=client.getMap(mapWithBasicCash + randomString());
  int size=1247;
  for (int i=0; i < size; i++) {
    map.put(i,i);
  }
  for (int i=0; i < size; i++) {
    map.get(i);
  }
  for (int i=0; i < size; i++) {
    map.remove(i);
    assertNull(map.get(i));
  }
}
