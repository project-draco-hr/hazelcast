{
  if (address == null) {
    if (DEBUG) {
      log("Address cannot be null");
      return null;
    }
  }
  MemberImpl member=getMember(address);
  if (member == null)   member=createMember(address,nodeType);
  addMember(member);
  return member;
}
