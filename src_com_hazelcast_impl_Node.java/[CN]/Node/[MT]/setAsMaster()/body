{
  masterAddress=address;
  if (DEBUG)   System.out.println("adding member myself");
  ClusterManager.get().addMember(address,getLocalNodeType());
  clusterImpl.setMembers(ClusterManager.get().lsMembers);
  unlock();
}
