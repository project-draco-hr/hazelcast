{
  masterAddress=address;
  if (DEBUG)   System.out.println("adding member myself");
  ClusterService.get().addMember(address,getLocalNodeType());
  clusterImpl.setMembers(ClusterService.get().lsMembers);
  unlock();
}
