{
  HazelcastInstance instance=getInstance();
  instance.getList("A");
  instance.getMap("A");
  instance.getMultiMap("A");
  instance.getQueue("A");
  instance.getSet("A");
  instance.getTopic("A");
  Collection<Instance> caches=instance.getInstances();
  assertEquals(6,caches.size());
}
