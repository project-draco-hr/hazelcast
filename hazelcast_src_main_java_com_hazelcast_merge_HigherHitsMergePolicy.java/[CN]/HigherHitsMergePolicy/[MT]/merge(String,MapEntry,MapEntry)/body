{
  if (existingEntry == null || existingEntry.getValue() == null) {
    return mergingEntry.getValue();
  }
  return (mergingEntry.getHits() > existingEntry.getHits()) ? mergingEntry.getValue() : existingEntry.getValue();
}
