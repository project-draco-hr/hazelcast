{
switch (evictionPolicy) {
case LFU:
    return LFU_POLICY;
case LRU:
  return LRU_POLICY;
case NONE:
return NULL_POLICY;
default :
throw new IllegalArgumentException("Unknown map eviction policy : " + evictionPolicy);
}
}
