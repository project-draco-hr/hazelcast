{
  if (this == other)   return true;
  return other != null && (this.name != null ? this.name.equals(other.name) : other.name == null) && this.backupCount == other.backupCount && this.evictionDelaySeconds == other.evictionDelaySeconds && this.evictionPercentage == other.evictionPercentage && this.maxIdleSeconds == other.maxIdleSeconds && (this.maxSize == other.maxSize || (Math.min(maxSize,other.maxSize) == 0 && Math.max(maxSize,other.maxSize) == Integer.MAX_VALUE)) && this.timeToLiveSeconds == other.timeToLiveSeconds && this.useBackupData == other.useBackupData && this.valueIndexed == other.valueIndexed;
}
