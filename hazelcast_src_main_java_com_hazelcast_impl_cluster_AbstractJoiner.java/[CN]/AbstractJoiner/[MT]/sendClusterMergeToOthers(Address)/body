{
  for (  MemberImpl member : node.getClusterService().getMemberList()) {
    if (!member.localMember()) {
      node.nodeService.createSingleInvocation(ClusterService.SERVICE_NAME,new MergeClusters(targetAddress),-1).setTarget(member.getAddress()).setTryCount(1).build().invoke();
    }
  }
}
