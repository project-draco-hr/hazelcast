{
  if (n <= 0) {
    return 0;
  }
  int skip=n;
  final int pos=position();
  final int cap=buffer.capacity();
  if (pos + skip > cap) {
    skip=cap - pos;
  }
  position(pos + skip);
  return skip;
}
