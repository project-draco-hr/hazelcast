{
  if (now) {
    doShutdown(force);
  }
 else {
    new Thread(new Runnable(){
      public void run(){
        ThreadContext.get().setCurrentInstance(hazelcastInstance);
        doShutdown(force);
      }
    }
).start();
  }
}
