{
  final Credentials credentials=(withCredentials && securityContext != null) ? securityContext.getCredentialsFactory().newCredentials() : null;
  return new JoinRequest(Packet.PACKET_VERSION,buildNumber,address,localMember.getUuid(),config,localNodeType,credentials,clusterService.getSize(),0);
}
