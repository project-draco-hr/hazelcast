{
  if (clusterService.getClusterState() != ClusterState.SHUTTING_DOWN) {
    throw new IllegalStateException("This method can be called only when cluster-state" + " is SHUTTING_DOWN!");
  }
  state=NodeState.SHUTTING_DOWN;
}
