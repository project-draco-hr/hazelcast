{
  if (!headComplete) {
    if (dst.remaining() < HEAD_SIZE) {
      return false;
    }
    dst.put(VERSION);
    dst.putShort(header);
    dst.putInt(partitionId);
    size=totalSize();
    dst.putInt(size);
    headComplete=true;
  }
  return writeValue(dst);
}
