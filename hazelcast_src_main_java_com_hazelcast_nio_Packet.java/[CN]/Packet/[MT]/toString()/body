{
  int keySize=(key == null) ? 0 : key.size();
  int valueSize=(value == null) ? 0 : value.size();
  return "Packet [" + operation + "] name="+ name+ ",local="+ local+ ",blockId="+ blockId+ ", keySize="+ keySize+ ", valueSize="+ valueSize+ " client="+ client;
}
