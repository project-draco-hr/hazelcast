{
  instance.getConfig().getMapConfig(mapName).setEvictionPolicy(MapConfig.EvictionPolicy.LRU).setMaxSizeConfig(new MaxSizeConfig(MAX_SIZE,MaxSizeConfig.MaxSizePolicy.PER_NODE));
  return instance.getMap(mapName);
}
