{
  return new PrivilegedExceptionAction<Void>(){
    public Void run(){
      handler.handle(node,protocol);
      node.clientHandlerService.getClientEndpoint(connection).removeRequest(ClientProtocolRequestHandler.this);
      return null;
    }
  }
;
}
