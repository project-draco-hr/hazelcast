{
  for (  MemberImpl member : node.getClusterImpl().getMemberList()) {
    if (!member.localMember()) {
      node.nodeService.createSingleInvocation(ClusterImpl.SERVICE_NAME,new MergeClusters(targetAddress),-1).setTarget(member.getAddress()).setTryCount(1).build().invoke();
    }
  }
}
