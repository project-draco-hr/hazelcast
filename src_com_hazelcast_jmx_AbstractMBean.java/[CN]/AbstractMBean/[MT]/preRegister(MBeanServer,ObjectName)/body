{
  try {
    instrument();
  }
 catch (  Exception e) {
    logger.log(Level.FINE,"Error generating MBeanInfo",e);
    throw e;
  }
  if (name != null) {
    objectName=name;
  }
  mbeanServer=server;
  return getObjectName();
}
