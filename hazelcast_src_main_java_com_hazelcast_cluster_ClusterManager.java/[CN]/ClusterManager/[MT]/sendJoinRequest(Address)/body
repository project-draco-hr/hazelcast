{
  if (toAddress == null) {
    toAddress=Node.get().getMasterAddress();
  }
  sendProcessableTo(new JoinRequest(thisAddress,Config.get().getGroupName(),Config.get().getGroupPassword(),Node.get().getLocalNodeType()),toAddress);
}
