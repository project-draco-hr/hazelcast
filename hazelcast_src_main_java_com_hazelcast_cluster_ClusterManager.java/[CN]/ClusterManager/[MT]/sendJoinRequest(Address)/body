{
  if (toAddress == null) {
    toAddress=node.getMasterAddress();
  }
  sendProcessableTo(new JoinRequest(thisAddress,node.getConfig().getGroupName(),node.getConfig().getGroupPassword(),node.getLocalNodeType()),toAddress);
}
