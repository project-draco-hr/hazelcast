{
  if (toAddress == null) {
    toAddress=node.getMasterAddress();
  }
  sendProcessableTo(new JoinRequest(thisAddress,Config.get().getGroupName(),Config.get().getGroupPassword(),node.getLocalNodeType()),toAddress);
}
