{
  if (toAddress == null) {
    toAddress=node.getMasterAddress();
  }
  sendProcessableTo(new JoinRequest(thisAddress,node.getConfig().getGroupConfig().getName(),node.getConfig().getGroupConfig().getPassword(),node.getLocalNodeType()),toAddress);
}
