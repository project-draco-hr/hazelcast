{
  enqueueAndWait(new Processable(){
    public void process(){
      joinReset();
      lsMembers.clear();
      mapMembers.clear();
      dataMemberCount.reset();
      memberMasterConfirmations.clear();
    }
  }
,5);
}
