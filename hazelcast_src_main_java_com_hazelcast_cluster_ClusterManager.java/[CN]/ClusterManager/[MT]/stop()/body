{
  if (setJoins != null) {
    setJoins.clear();
  }
  timeToStartJoin=0;
  if (lsMembers != null) {
    lsMembers.clear();
  }
  dataMemberCount.reset();
  if (mapMembers != null) {
    mapMembers.clear();
  }
  if (lsMembersBefore != null) {
    lsMembersBefore.clear();
  }
  if (mapCalls != null) {
    mapCalls.clear();
  }
}
