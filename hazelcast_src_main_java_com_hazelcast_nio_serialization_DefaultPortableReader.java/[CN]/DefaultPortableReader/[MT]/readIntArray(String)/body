{
  int pos=getPosition(fieldName);
  in.position(pos);
  final int len=in.readInt();
  final int[] values=new int[len];
  for (int i=0; i < len; i++) {
    values[i]=in.readInt();
  }
  return values;
}
