{
  assertTrue("syncDelay is " + synDelay,synDelay.get() >= (1 - BasicBackPressureService.RANGE) * SYNC_WINDOW);
  assertTrue("syncDelay is " + synDelay,synDelay.get() <= (1 + BasicBackPressureService.RANGE) * SYNC_WINDOW);
}
