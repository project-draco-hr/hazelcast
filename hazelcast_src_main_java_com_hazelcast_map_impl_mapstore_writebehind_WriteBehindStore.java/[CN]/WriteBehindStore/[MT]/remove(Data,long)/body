{
  if (NATIVE == inMemoryFormat) {
    key=toData(key);
  }
  long storeTime=now + writeDelayMillis;
  DelayedEntry<Data,Object> delayedEntry=DelayedEntries.createWithoutValue(key,storeTime,partitionId);
  add(delayedEntry);
}
