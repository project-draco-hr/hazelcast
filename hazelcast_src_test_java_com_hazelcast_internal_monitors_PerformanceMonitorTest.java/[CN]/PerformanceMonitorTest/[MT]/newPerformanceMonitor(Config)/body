{
  HazelcastInstance hz=createHazelcastInstance(config);
  NodeEngineImpl nodeEngineImpl=getNodeEngineImpl(hz);
  return new PerformanceMonitor(hz,Logger.getLogger(PerformanceMonitor.class),nodeEngineImpl.getNode().getHazelcastThreadGroup(),nodeEngineImpl.getNode().groupProperties);
}
