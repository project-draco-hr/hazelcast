{
  PerformanceMonitor performanceMonitor=newPerformanceMonitor(new Config().setProperty(PerformanceMonitor.ENABLED.getName(),"true"));
  performanceMonitor.start();
  performanceMonitor.register(null);
}
