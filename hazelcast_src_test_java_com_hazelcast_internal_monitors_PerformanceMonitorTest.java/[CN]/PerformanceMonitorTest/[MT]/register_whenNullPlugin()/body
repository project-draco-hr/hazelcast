{
  PerformanceMonitor performanceMonitor=newPerformanceMonitor(new Config().setProperty(PERFORMANCE_MONITOR_ENABLED.getName(),"true"));
  performanceMonitor.start();
  performanceMonitor.register(null);
}
