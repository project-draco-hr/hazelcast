{
  PerformanceMonitor performanceMonitor=newPerformanceMonitor(new Config().setProperty(PERFORMANCE_MONITOR_ENABLED.getName(),"false"));
  performanceMonitor.start();
  assertNull(performanceMonitor.performanceLog);
}
