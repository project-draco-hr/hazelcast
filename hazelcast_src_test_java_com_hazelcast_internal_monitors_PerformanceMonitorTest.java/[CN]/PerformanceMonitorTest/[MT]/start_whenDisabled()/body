{
  PerformanceMonitor performanceMonitor=newPerformanceMonitor(new Config().setProperty(PerformanceMonitor.ENABLED.getName(),"false"));
  performanceMonitor.start();
  assertNull(performanceMonitor.performanceLog);
}
