{
  if (!sent.compareAndSet(false,true)) {
    throw new IllegalStateException("Response already sent for callback: " + callback + ", current-response: : "+ obj+ " -vs- "+ response,trace);
  }
  response=obj;
  trace=new Throwable();
  callback.notify(obj);
}
