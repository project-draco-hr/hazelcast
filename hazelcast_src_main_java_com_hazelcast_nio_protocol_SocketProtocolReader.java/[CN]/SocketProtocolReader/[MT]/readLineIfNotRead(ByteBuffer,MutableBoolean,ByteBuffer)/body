{
  while (!lineIsRead.get() && bb.hasRemaining()) {
    byte b=bb.get();
    char c=(char)b;
    if (c == '\n') {
      lineIsRead.set(true);
    }
 else     if (c != '\r') {
      line.put(b);
    }
  }
}
