{
  Config config=new Config();
  config.setProperty(GroupProperties.PROP_SLOW_OPERATION_DETECTOR_THRESHOLD_MILLIS,String.valueOf(slowOperationThresholdMillis));
  return createHazelcastInstance(config);
}
