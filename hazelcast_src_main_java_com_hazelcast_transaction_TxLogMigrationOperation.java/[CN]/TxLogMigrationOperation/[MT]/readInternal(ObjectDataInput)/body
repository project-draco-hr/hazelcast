{
  super.readInternal(in);
  int len=in.readInt();
  logs=new ArrayList<TransactionLog>(len);
  if (len > 0) {
    for (int i=0; i < len; i++) {
      TransactionLog log=new TransactionLog();
      log.readData(in);
      logs.add(log);
    }
  }
}
