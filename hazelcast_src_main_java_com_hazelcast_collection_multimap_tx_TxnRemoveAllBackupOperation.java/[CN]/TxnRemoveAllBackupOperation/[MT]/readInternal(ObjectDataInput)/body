{
  super.readInternal(in);
  caller=in.readUTF();
  threadId=in.readInt();
  int size=in.readInt();
  recordIds=new ArrayList<Long>();
  for (int i=0; i < size; i++) {
    recordIds.add(in.readLong());
  }
}
