{
  final ITopic<Object> t=client.getTopic("stuff");
  t.addMessageListener(new MessageListener(){
    public void onMessage(    Message message){
      System.out.println("hi");
    }
  }
);
  for (int i=0; i < 1000; i++)   t.publish(i);
}
