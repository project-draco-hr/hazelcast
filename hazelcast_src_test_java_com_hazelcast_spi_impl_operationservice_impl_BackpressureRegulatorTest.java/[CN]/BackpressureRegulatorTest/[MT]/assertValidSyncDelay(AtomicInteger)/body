{
  assertTrue("syncDelayCounter is " + synDelay,synDelay.get() >= (1 - BackpressureRegulator.RANGE) * SYNC_WINDOW);
  assertTrue("syncDelayCounter is " + synDelay,synDelay.get() <= (1 + BackpressureRegulator.RANGE) * SYNC_WINDOW);
}
