{
  try {
    final Invocation inv=obtainServiceInvocation();
    inv.set(name,operation,key,value);
    inv.timeout=timeout;
    return inv;
  }
 catch (  final Exception e) {
    e.printStackTrace();
  }
  return null;
}
