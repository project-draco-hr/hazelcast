{
  try {
    Invocation inv=obtainServiceInvocation();
    inv.set(name,operation,key,value);
    inv.timeout=timeout;
    return inv;
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
  return null;
}
