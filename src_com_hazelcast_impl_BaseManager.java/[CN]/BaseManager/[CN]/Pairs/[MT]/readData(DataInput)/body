{
  int size=in.readInt();
  for (int i=0; i < size; i++) {
    if (lsKeyValues == null) {
      lsKeyValues=new ArrayList<KeyValue>();
    }
    KeyValue kv=new KeyValue();
    kv.readData(in);
    lsKeyValues.add(kv);
  }
}
