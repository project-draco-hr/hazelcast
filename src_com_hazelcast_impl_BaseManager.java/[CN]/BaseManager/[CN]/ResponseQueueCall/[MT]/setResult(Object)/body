{
  try {
    if (obj == null) {
      responses.add(OBJECT_NULL);
    }
 else {
      responses.add(obj);
    }
  }
 catch (  Throwable e) {
    logger.log(Level.FINEST,"Exception when handling " + ResponseQueueCall.this,e);
    e.printStackTrace();
  }
}
