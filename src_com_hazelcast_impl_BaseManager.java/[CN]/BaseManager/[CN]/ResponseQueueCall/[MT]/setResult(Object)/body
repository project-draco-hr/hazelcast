{
  try {
    if (obj == null) {
      responses.add(OBJECT_NULL);
    }
 else {
      responses.add(obj);
    }
  }
 catch (  Throwable e) {
    System.out.println("Exception when handling " + ResponseQueueCall.this);
    e.printStackTrace();
  }
}
