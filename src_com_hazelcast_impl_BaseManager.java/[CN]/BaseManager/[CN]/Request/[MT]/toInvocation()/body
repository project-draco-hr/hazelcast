{
  Invocation inv=obtainServiceInvocation();
  inv.local=false;
  inv.operation=operation;
  inv.name=name;
  if (key != null)   inv.doHardCopy(key,inv.key);
  if (value != null)   inv.doHardCopy(value,inv.data);
  inv.blockId=blockId;
  inv.timeout=timeout;
  inv.txnId=txnId;
  inv.eventId=eventId;
  inv.threadId=lockThreadId;
  inv.lockAddress=lockAddress;
  inv.lockCount=lockCount;
  inv.longValue=longValue;
  inv.recordId=recordId;
  return inv;
}
