{
  Request copy=new Request();
  copy.set(local,operation,name,key,value,blockId,timeout,txnId,eventId,lockThreadId,lockAddress,lockCount,caller,longValue,recordId);
  copy.attachment=attachment;
  copy.response=response;
  copy.scheduled=scheduled;
  return copy;
}
