{
  reset();
  set(false,inv.operation,inv.name,doTake(inv.key),doTake(inv.value),inv.blockId,inv.timeout,inv.txnId,inv.eventId,inv.threadId,inv.lockAddress,inv.lockCount,inv.conn.getEndPoint(),inv.longValue,inv.recordId);
}
