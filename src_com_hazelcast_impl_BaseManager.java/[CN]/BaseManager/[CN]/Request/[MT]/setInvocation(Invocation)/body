{
  reset();
  set(false,inv.operation,inv.name,inv.doTake(inv.key),inv.doTake(inv.data),inv.blockId,inv.timeout,inv.txnId,inv.eventId,inv.threadId,inv.lockAddress,inv.lockCount,inv.conn.getEndPoint(),inv.longValue,inv.recordId);
}
