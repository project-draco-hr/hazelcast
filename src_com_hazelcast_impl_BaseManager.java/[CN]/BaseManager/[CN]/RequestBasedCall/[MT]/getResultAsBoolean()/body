{
  try {
    Object result=getResult();
    if (result == OBJECT_NULL || result == null) {
      return false;
    }
    if (result == Boolean.TRUE)     return true;
 else     return false;
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
 finally {
    this.request.reset();
  }
  return false;
}
