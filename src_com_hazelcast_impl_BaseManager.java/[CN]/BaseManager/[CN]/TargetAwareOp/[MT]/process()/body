{
  setTarget();
  if (target == null) {
    if (isSuperClient()) {
      setResult(OBJECT_REDO);
      return;
    }
 else {
      throw new RuntimeException("Target cannot be null!");
    }
  }
  if (target.equals(thisAddress)) {
    doLocalOp();
  }
 else {
    invoke();
  }
}
