{
  txnId=in.readUTF();
  int size=in.readInt();
  for (int i=0; i < size; i++) {
    Data key=new Data();
    key.readData(in);
    TransactionLogItem logItem=new TransactionLogItem();
    logItem.readData(in);
    changes.put(key,logItem);
  }
}
