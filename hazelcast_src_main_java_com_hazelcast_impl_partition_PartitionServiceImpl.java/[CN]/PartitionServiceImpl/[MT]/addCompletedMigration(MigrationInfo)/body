{
  lock.lock();
  try {
    if (completedMigrations.size() > 10) {
      completedMigrations.removeFirst();
    }
    completedMigrations.add(migrationInfo);
  }
  finally {
    lock.unlock();
  }
}
