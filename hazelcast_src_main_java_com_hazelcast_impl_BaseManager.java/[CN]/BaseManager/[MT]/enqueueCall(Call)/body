{
  if (maxOperationLimit > 0 && Thread.currentThread().getThreadGroup() != node.threadGroup && mapCalls.size() >= maxOperationLimit) {
    throw new OperationRejectedException("Max concurrent operation limit exceeded! -> " + maxOperationLimit);
  }
  call.onEnqueue();
  enqueueAndReturn(call);
}
