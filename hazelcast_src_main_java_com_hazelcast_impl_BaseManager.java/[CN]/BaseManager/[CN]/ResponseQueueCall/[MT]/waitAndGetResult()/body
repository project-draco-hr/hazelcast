{
  while (true) {
    try {
      Object obj=responses.poll(5,TimeUnit.SECONDS);
      if (obj != null) {
        return obj;
      }
      node.checkNodeState();
    }
 catch (    InterruptedException e) {
      handleInterruptedException();
    }
  }
}
