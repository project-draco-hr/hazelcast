{
  if (packet.responseType == RESPONSE_SUCCESS) {
    setResult(packet.longValue);
  }
 else {
    throw new HazelcastException("handleLongNoneRedoResponse.responseType " + packet.responseType);
  }
}
