{
  try {
    node.listenerManager.callListeners(this);
    if (dataKey != null) {
      dataKey.setNoData();
    }
    if (dataValue != null) {
      dataValue.setNoData();
    }
  }
 catch (  final Exception e) {
    e.printStackTrace();
  }
}
