{
  final boolean dirty=version.get() != updates.get();
  final int count;
  if (dirty) {
    count=dirtyCount.incrementAndGet();
  }
 else {
    dirtyCount.set(0);
    count=0;
  }
  return count >= 10;
}
