{
  if (target.equals(nodeEngine.getThisAddress())) {
    if (logger.isFinestEnabled()) {
      logger.finest("This node is now owner of partition, cannot sync replica -> partitionId: " + partitionId + ", replicaIndex: "+ replicaIndex+ ", partition-info: "+ getPartitionImpl(partitionId));
    }
    return false;
  }
  return true;
}
