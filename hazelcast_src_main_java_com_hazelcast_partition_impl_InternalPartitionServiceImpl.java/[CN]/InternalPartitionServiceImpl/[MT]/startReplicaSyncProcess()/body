{
  int c=replicaSyncProcessCount.get();
  if (c >= maxParallelReplications) {
    return false;
  }
  c=replicaSyncProcessCount.incrementAndGet();
  if (c >= maxParallelReplications) {
    replicaSyncProcessCount.decrementAndGet();
    return false;
  }
  return true;
}
