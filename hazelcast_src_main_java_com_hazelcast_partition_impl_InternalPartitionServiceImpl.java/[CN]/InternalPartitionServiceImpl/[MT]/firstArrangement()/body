{
  if (initialized) {
    return;
  }
  if (!node.isMaster()) {
    notifyMasterToAssignPartitions();
    return;
  }
  lock.lock();
  try {
    if (initialized) {
      return;
    }
    if (initializePartitionAssignments()) {
      return;
    }
    publishPartitionRuntimeState();
  }
  finally {
    lock.unlock();
  }
}
