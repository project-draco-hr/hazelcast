{
  boolean isDs=throwable instanceof DataSerializable;
  out.writeBoolean(isDs);
  if (isDs) {
    out.writeUTF(throwable.getClass().getName());
    ((DataSerializable)throwable).writeData(out);
  }
 else   out.writeUTF(throwable.getMessage());
}
