{
  beforeProcess();
  Operation op=prepareOperation();
  op.setCallerUuid(endpoint.getUuid());
  InvocationBuilder builder=nodeEngine.getOperationService().createInvocationBuilder(getServiceName(),op,getPartitionId()).setTryCount(TRY_COUNT).setResultDeserialized(false);
  ICompletableFuture future=builder.invoke();
  future.andThen(this);
}
