{
  out.writeUTF(name);
  out.writeBoolean(isRemoveAll);
  out.writeBoolean(keys != null);
  if (keys != null) {
    out.write(keys.size());
    for (    Data key : keys) {
      key.writeData(out);
    }
  }
}
