{
  out.writeUTF(name);
  out.writeBoolean(isRemoveAll);
  out.writeBoolean(keys != null);
  if (keys != null) {
    out.writeInt(keys.size());
    for (    Data key : keys) {
      key.writeData(out);
    }
  }
}
