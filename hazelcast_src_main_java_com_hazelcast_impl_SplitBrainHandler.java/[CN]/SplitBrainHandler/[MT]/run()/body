{
  if (node.isMaster() && node.joined() && node.isActive()&& node.clusterImpl.shouldTryMerge()&& inProgress.compareAndSet(false,true)) {
    try {
      searchForOtherClusters();
    }
  finally {
      inProgress.set(false);
    }
  }
}
