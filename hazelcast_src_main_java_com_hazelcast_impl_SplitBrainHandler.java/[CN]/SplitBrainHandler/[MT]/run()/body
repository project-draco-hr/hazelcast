{
  if (node.isMaster() && node.joined() && node.isActive()&& node.clusterService.shouldTryMerge()&& inProgress.compareAndSet(false,true)) {
    try {
      searchForOtherClusters();
    }
  finally {
      inProgress.set(false);
    }
  }
}
