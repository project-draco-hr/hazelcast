{
  executionEnds.count(elapsed);
  if (elapsed > maxCompletionTime.get())   maxCompletionTime.set(elapsed);
  if (elapsed < minCompletionTime.get())   minCompletionTime.set(elapsed);
  publishSubResult();
}
