{
  String name=protocol.args[0];
  List<MessageListener> list=messageListeners.get(name);
  if (list != null) {
    for (    MessageListener<Object> messageListener : list) {
      messageListener.onMessage(new DataMessage(name,new Data(SerializationConstants.CONSTANT_TYPE_BYTE_ARRAY,protocol.buffers[0].array()),serializationService));
    }
  }
}
