{
  if (task == null)   throw new NullPointerException();
  if (isShutdown()) {
    throw new RejectedExecutionException(getRejectionMessage());
  }
  final NodeEngine nodeEngine=getNodeEngine();
  final String uuid=UUID.randomUUID().toString();
  final boolean sync=!preventSync && checkSync();
  final Future future=nodeEngine.getOperationService().invokeOnPartition(DistributedExecutorService.SERVICE_NAME,new CallableTaskOperation(name,uuid,task),partitionId);
  if (sync) {
    Object response;
    try {
      response=future.get();
    }
 catch (    Exception e) {
      response=e;
    }
    return new CompletedFuture<T>(nodeEngine.getSerializationService(),response);
  }
  return new CancellableDelegatingFuture<T>(future,nodeEngine,uuid,partitionId);
}
