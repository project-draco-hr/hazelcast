{
  Data value=event.getValue();
  Data oldValue=event.getOldValue();
  Data mergingValue=event.getMergingValue();
  Data key=event.getKey();
  return new DataAwareEntryEvent(member,event.getEventType().getType(),name,key,value,oldValue,mergingValue,getContext().getSerializationService());
}
