{
  check(e);
  checkTime(l,timeUnit);
  l=(l < 0) ? 0 : l;
  if (e == null) {
    throw new NullPointerException();
  }
  return proxyHelper.doCommandAsBoolean(null,Command.QOFFER,new String[]{getName(),String.valueOf(timeUnit.toMillis(l))},proxyHelper.toData(e));
}
