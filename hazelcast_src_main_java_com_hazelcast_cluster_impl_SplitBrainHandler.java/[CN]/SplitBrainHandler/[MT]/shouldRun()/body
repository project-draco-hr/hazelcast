{
  if (!node.joined()) {
    return false;
  }
  if (!node.isMaster()) {
    return false;
  }
  if (node.getState() != NodeState.ACTIVE) {
    return false;
  }
  if (node.clusterService.isJoinInProgress()) {
    return false;
  }
  return true;
}
