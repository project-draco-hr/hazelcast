{
  if (index() == 0) {
    initFrameSize(src);
  }
  while (index() >= Bits.INT_SIZE_IN_BYTES && src.hasRemaining() && !isComplete()) {
    accumulate(src,getFrameLength() - index());
  }
  return isComplete();
}
