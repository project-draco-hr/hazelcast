{
  final StringBuilder b=new StringBuilder(size() * 3 + 2);
  b.append('{');
  String separator="";
  for (  long i : values) {
    if (i == missingValue) {
      continue;
    }
    b.append(i).append(separator);
    separator=",";
  }
  return b.append('}').toString();
}
