{
  assertEqualsEventually(new Callable<NodeState>(){
    @Override public NodeState call() throws Exception {
      final NodeState state=node.getState();
      return state;
    }
  }
,NodeState.PASSIVE);
}
