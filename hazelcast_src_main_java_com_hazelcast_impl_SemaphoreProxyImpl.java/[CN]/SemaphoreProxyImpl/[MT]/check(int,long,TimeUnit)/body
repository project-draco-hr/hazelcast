{
  check(permits);
  if (timeout < -1)   throw new IllegalArgumentException("Invalid timeout value: " + timeout);
  if (timeunit == null) {
    throw new NullPointerException("TimeUnit can not be null.");
  }
}
