{
  String xml="<hazelcast>\n" + "   <security>       " + "   </security>"+ "   <security>   "+ "   </security>"+ "</hazelcast>";
  ByteArrayInputStream bis=new ByteArrayInputStream(xml.getBytes());
  XmlConfigBuilder configBuilder=new XmlConfigBuilder(bis);
  configBuilder.build();
}
