{
  byte x=0;
  x=setTrue(x,1);
  x=setTrue(x,3);
  x=setTrue(x,6);
  for (byte i=0; i < 8; i++) {
    boolean get=isTrue(x,i);
    System.out.println(i + " " + get);
  }
  for (byte i=0; i < 8; i++) {
    boolean get=isTrue(x,i);
    System.out.println(i + " " + get);
  }
}
