{
  Object response=null;
  try {
    super.run();
    if (!isCancelled()) {
      response=get();
    }
  }
 catch (  Exception e) {
    logException(e);
    response=e;
  }
 finally {
    if (!isCancelled()) {
      setResponse(response);
    }
  }
}
