{
  AssertionError error=null;
  for (int k=0; k < timeoutSeconds; k++) {
    try {
      task.run();
      return;
    }
 catch (    AssertionError e) {
      error=e;
    }
    sleepSeconds(1);
  }
  printAllStackTraces();
  throw error;
}
