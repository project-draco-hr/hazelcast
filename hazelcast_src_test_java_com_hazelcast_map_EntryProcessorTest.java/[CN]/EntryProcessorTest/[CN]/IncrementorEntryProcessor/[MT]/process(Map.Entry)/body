{
  Integer value=(Integer)entry.getValue();
  if (value == -1) {
    entry.setValue(null);
    return null;
  }
  if (value == null) {
    value=0;
  }
  value++;
  entry.setValue(value);
  return value;
}
