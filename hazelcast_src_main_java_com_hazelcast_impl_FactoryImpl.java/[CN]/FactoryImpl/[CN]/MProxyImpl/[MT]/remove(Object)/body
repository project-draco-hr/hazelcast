{
  try {
    return mproxyReal.remove(key);
  }
 catch (  Throwable e) {
    if (factory.restarted) {
      return remove(key);
    }
 else {
      throw (RuntimeException)e;
    }
  }
 finally {
    afterCall();
  }
}
