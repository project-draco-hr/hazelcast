{
  beforeCall();
  try {
    return mproxyReal.get(key);
  }
 catch (  Throwable e) {
    if (factory.restarted) {
      return get(key);
    }
 else {
      throw (RuntimeException)e;
    }
  }
 finally {
    afterCall();
  }
}
