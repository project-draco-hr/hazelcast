{
  if (keys.size() > 0) {
    Map map=cmap.loader.loadAll(keys);
    if (map != null && map.size() > 0) {
      Set<Map.Entry> entries=map.entrySet();
      for (      Map.Entry entry : entries) {
        if (!mProxy.putFromLoad(entry.getKey(),entry.getValue()))         break;
      }
    }
  }
}
