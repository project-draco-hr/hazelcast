{
  if (time == 0 || unit == null) {
    return 0;
  }
 else   if (time < 0) {
    return -1;
  }
 else {
    long millis=unit.toMillis(time);
    if (millis < 1) {
      millis=-1;
    }
    return millis;
  }
}
