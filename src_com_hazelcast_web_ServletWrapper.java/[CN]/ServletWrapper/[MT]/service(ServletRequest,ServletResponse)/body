{
  if (!(req instanceof RequestWrapper)) {
    ServletRequest reqHazel=(ServletRequest)req.getAttribute(WebFilter.HAZELCAST_REQUEST);
    if (reqHazel != null)     req=reqHazel;
  }
  base.service(req,res);
}
