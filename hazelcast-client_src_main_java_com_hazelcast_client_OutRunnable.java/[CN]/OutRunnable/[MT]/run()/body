{
  while (true) {
    try {
      Call c=queue.take();
      writer.write(c.getRequest());
      callMap.put(c.getId(),c);
    }
 catch (    InterruptedException e) {
      e.printStackTrace();
    }
  }
}
