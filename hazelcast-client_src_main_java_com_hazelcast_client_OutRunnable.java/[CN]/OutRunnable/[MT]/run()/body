{
  while (true) {
    try {
      Call c=queue.take();
      callMap.put(c.getId(),c);
      writer.write(c.getRequest());
    }
 catch (    InterruptedException e) {
      e.printStackTrace();
    }
  }
}
