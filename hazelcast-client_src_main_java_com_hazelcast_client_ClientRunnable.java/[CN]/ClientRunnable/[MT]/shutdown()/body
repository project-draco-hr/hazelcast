{
  if (!running || terminated) {
    return;
  }
synchronized (monitor) {
    running=false;
    while (!terminated) {
      try {
        monitor.wait();
      }
 catch (      InterruptedException ignored) {
      }
    }
  }
}
