{
  assertTrue("syncDelay is " + synDelay,synDelay.get() >= (1 - BackPressureService.RANGE) * SYNC_WINDOW);
  assertTrue("syncDelay is " + synDelay,synDelay.get() <= (1 + BackPressureService.RANGE) * SYNC_WINDOW);
}
