{
  if (this == obj) {
    return true;
  }
  if (!(obj instanceof CacheConfig)) {
    return false;
  }
  if (!super.equals(obj)) {
    return false;
  }
  return equalsInternal((CacheConfig)obj);
}
