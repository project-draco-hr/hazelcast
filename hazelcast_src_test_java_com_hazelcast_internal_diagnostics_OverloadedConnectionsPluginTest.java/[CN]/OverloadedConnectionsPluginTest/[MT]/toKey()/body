{
  assertToKey(DummyOperation.class.getName(),new DummyOperation());
  assertToKey(Integer.class.getName(),new Integer(10));
  assertToKey(Backup.class.getName() + "#" + DummyOperation.class.getName(),new Backup(new DummyOperation(),getAddress(local),new long[0],true));
}
