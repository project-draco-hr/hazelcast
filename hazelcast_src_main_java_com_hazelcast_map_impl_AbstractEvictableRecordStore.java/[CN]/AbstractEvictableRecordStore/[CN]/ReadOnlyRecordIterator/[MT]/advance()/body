{
  final long now=this.now;
  while (iterator.hasNext()) {
    nextRecord=iterator.next();
    if (nextRecord != null) {
      if (checkExpiration) {
        if (!isExpired(nextRecord,now)) {
          return;
        }
      }
 else {
        return;
      }
    }
  }
  nextRecord=null;
}
