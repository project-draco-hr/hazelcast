{
  final int size=size();
  if (size < 1) {
    return 0;
  }
  final int evictableSize=getEvictionOperator().evictableSize(this);
  if (evictableSize < 1) {
    return 0;
  }
  return evictableSize;
}
