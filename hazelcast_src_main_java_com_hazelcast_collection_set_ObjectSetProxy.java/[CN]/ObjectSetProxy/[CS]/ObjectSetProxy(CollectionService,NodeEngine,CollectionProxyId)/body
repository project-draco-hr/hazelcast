{
  super(service,nodeEngine,nodeEngine.getConfig().getMultiMapConfig("set:" + proxyId.getKeyName()).setValueCollectionType(MultiMapConfig.ValueCollectionType.SET),proxyId);
  this.key=nodeEngine.toData(proxyId.getKeyName());
}
