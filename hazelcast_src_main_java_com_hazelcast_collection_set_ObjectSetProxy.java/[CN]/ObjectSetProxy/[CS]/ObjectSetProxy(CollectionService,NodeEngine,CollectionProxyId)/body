{
  super(service,nodeEngine,nodeEngine.getConfig().getMultiMapConfig(COLLECTION_SET_NAME + proxyId.getKeyName()).setValueCollectionType(MultiMapConfig.ValueCollectionType.SET),proxyId);
  this.key=nodeEngine.toData(proxyId.getKeyName());
}
