{
  Integer value=entry.getValue();
  if (value == null) {
    return Boolean.TRUE;
  }
  int remaining=value - 1;
  if (remaining <= 0) {
    entry.setValue(null);
    return Boolean.TRUE;
  }
  entry.setValue(remaining);
  return Boolean.FALSE;
}
