{
  setCurrentTake();
  return (blCurrentTake != null) && (blCurrentTake.blockId == blockId) && (thisAddress.equals(blCurrentTake.address))&& (blCurrentTake.containsValidItem() || !blCurrentTake.isFull());
}
