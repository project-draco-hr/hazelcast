{
  QData qdata=queue.poll();
  if (qdata == null) {
    return null;
  }
  long now=System.currentTimeMillis();
  if (isValid(qdata,now)) {
    return qdata;
  }
  while (qdata != null) {
    qdata=queue.poll();
    if (isValid(qdata,now)) {
      return qdata;
    }
  }
  return qdata;
}
