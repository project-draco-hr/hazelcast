{
  if (keys.add(key)) {
    if (leases.size() > 0) {
      leases.remove(0);
    }
    if (last) {
      queue.offer(new QData(key));
    }
 else {
      queue.offerFirst(new QData(key));
    }
    takeOne();
  }
}
