{
  Data keyData=req.key;
  int i=-1;
  for (  QData qdata : queue) {
    if (qdata.data.equals(keyData)) {
      i++;
      break;
    }
 else {
      i++;
    }
  }
  req.response=toData(i);
  returnResponse(req);
}
