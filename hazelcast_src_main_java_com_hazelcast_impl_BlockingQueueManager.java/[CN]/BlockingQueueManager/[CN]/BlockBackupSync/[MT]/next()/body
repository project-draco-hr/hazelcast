{
  while (true) {
    QData data=block.get(index);
    if (data != null) {
      return data.data;
    }
    index++;
    if (index > indexUpto)     return null;
    if (index >= BLOCK_SIZE)     return null;
  }
}
