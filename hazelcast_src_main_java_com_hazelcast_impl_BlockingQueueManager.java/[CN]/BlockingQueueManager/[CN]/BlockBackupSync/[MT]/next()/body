{
  while (true) {
    Data data=block.get(index);
    if (data != null) {
      return data;
    }
    index++;
    if (index > indexUpto)     return null;
    if (index >= BLOCK_SIZE)     return null;
  }
}
