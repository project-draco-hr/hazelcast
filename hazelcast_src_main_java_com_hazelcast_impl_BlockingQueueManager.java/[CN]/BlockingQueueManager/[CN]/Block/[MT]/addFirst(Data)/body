{
  final QData data=new QData(item);
  List<QData> ordered=new ArrayList<QData>(BLOCK_SIZE);
  for (  QData d : values) {
    if (d != null) {
      ordered.add(d);
    }
  }
  values[0]=data;
  int i=1;
  for (  QData d : ordered) {
    values[i++]=d;
  }
  addIndex=i;
  removeIndex=0;
  return 0;
}
