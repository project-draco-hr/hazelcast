{
  if (isMaster() && ready()) {
    BQ bq=getOrCreateBQ(req.name);
    bq.doAddKey(req.key);
    req.key=null;
    req.response=Boolean.TRUE;
    returnResponse(req);
  }
 else {
    returnRedoResponse(req);
  }
}
