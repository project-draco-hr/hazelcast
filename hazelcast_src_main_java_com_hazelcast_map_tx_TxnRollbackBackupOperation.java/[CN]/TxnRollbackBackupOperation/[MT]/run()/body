{
  if (recordStore.isLocked(getKey()) && !recordStore.unlock(getKey(),lockOwner,lockThreadId)) {
    throw new TransactionException("Lock is not owned by the transaction !" + lockOwner + " thId: "+ lockThreadId);
  }
}
