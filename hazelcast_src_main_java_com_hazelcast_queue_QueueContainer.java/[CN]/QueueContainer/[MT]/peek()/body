{
  QueueItem item=itemQueue.peek();
  if (item == null) {
    return null;
  }
  Data data=item.getData();
  if (data == null) {
    data=store.load(item.getItemId(),fromBackup);
    item.setData(data);
  }
  return data;
}
