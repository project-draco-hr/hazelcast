{
  partitionId=in.readInt();
  name=in.readUTF();
  int size=in.readInt();
  for (int j=0; j < size; j++) {
    QueueItem item=new QueueItem();
    item.readData(in);
    itemQueue.offer(item);
    idGen++;
  }
}
