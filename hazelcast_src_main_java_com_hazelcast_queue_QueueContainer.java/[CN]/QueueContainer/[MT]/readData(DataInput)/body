{
  partitionId=in.readInt();
  name=in.readUTF();
  int size=in.readInt();
  for (int j=0; j < size; j++) {
    Data data=new Data();
    data.readData(in);
    QueueItem item=new QueueItem(data);
    itemQueue.offer(item);
  }
}
