{
  QueueItem item=peek();
  if (store.isEnabled()) {
    try {
      store.delete(item.getItemId());
    }
 catch (    Exception e) {
      throw new RetryableException(e);
    }
  }
  itemQueue.poll();
  return item.getData();
}
