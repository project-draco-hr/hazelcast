{
  sbState.append("\nIndex- records: " + records.size() + ", mapIndexes:"+ mapIndexes.size()+ ", indexTypes:"+ ((indexTypes == null) ? 0 : indexTypes.length));
  for (  Index index : mapIndexes.values()) {
    index.appendState(sbState);
  }
}
