{
  if (service == null) {
    service=nodeService.getService(serviceName);
    if (service == null) {
      throw new RuntimeException(serviceName + "  not found!");
    }
  }
  return (T)service;
}
