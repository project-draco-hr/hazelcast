{
  if (!closeTriggered) {
    releaseCacheManager(uri,classLoaderReference.get());
    for (    ICache cache : caches.values()) {
      cache.close();
    }
    caches.clear();
  }
  closeTriggered=true;
}
