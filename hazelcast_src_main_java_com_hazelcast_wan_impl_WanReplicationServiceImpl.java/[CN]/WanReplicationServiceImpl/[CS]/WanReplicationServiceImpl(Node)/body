{
  this.node=node;
  this.logger=node.getLogger(WanReplicationServiceImpl.class.getName());
  this.executor=new StripedExecutor(node.getLogger(WanReplicationServiceImpl.class),node.getThreadNamePrefix("wan"),node.threadGroup,ExecutorConfig.DEFAULT_POOL_SIZE,ExecutorConfig.DEFAULT_QUEUE_CAPACITY);
}
