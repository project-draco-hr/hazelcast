{
  beforeCall();
  try {
    return mproxyReal.remove(key);
  }
 catch (  Throwable e) {
    if (factory.restarted) {
      return remove(key);
    }
    Util.throwUncheckedException(e);
    return null;
  }
 finally {
    afterCall();
  }
}
