{
  beforeCall();
  try {
    return mproxyReal.get(key);
  }
 catch (  Throwable e) {
    if (factory.restarted) {
      return get(key);
    }
    Util.throwUncheckedException(e);
    return null;
  }
 finally {
    afterCall();
  }
}
