{
  response=false;
  try {
    dataMap=getContainer().compareAndRemove(dataList,retain);
    if (dataMap.size() > 0) {
      response=true;
      deleteFromStore(false);
    }
  }
 catch (  Exception e) {
  }
}
