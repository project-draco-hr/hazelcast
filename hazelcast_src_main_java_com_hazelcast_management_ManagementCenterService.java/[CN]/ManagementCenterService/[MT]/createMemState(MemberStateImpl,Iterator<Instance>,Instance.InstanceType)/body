{
  int count=0;
  while (it.hasNext()) {
    Instance proxyObject=it.next();
    if (proxyObject.getInstanceType() == type) {
      if (count < maxVisibleInstanceCount) {
      }
      it.remove();
    }
  }
}
