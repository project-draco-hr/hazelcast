{
  oldValue=null;
  boolean locked=booleanCall(ClusterOperation.CONCURRENT_MAP_LOCK_RETURN_OLD,name,key,null,timeout,txnId,-1);
  if (locked) {
    backup(CONCURRENT_MAP_BACKUP_LOCK);
  }
  return locked;
}
