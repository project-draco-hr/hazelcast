{
  if (hardCopy) {
    backupOp.sendBackup(operation,bi.name,doHardCopy(bi.key),doHardCopy(bi.value),bi.owner,bi.lockCount,distance);
  }
 else {
    backupOp.sendBackup(operation,bi.name,bi.key,bi.value,bi.owner,bi.lockCount,distance);
  }
}
