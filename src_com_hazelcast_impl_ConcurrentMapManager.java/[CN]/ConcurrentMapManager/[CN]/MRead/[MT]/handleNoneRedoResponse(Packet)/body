{
  removeCall(getId());
  lastReadRecordId=packet.recordId;
  Data key=doTake(packet.key);
  if (key == null) {
    setResult(null);
  }
 else {
    Data value=null;
    if (containsValue) {
      value=doTake(packet.value);
    }
    setResult(new SimpleDataEntry(request.name,request.blockId,key,value,(int)packet.longValue));
  }
}
