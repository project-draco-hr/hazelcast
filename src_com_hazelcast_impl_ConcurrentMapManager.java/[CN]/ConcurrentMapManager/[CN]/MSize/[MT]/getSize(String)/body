{
  reset();
  shouldRedo=false;
  total=0;
  setLocal(OP_CMAP_SIZE,name,null,null,0,-1,-1);
  doOp();
  if (shouldRedo) {
    try {
      Thread.sleep(2000);
    }
 catch (    InterruptedException e) {
      e.printStackTrace();
    }
    return getSize(name);
  }
  return total;
}
