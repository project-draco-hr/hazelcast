{
  Data key=(hardCopy) ? doHardCopy(bi.key) : bi.key;
  Data value=(hardCopy) ? doHardCopy(bi.value) : bi.value;
  sendBackup(operation,bi.name,key,value,bi.owner,bi.lockCount,-1,bi.version,bi.copyCount,distance);
}
