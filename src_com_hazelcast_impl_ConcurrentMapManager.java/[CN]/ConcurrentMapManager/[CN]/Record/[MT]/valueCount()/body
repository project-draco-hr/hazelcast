{
  int count=0;
  if (value != null) {
    count=1;
    if (copyCount != null) {
      count+=copyCount.get();
    }
  }
 else   if (lsValues != null) {
    count=lsValues.size();
  }
 else {
    if (copyCount != null) {
      count+=copyCount.get();
    }
  }
  return count;
}
