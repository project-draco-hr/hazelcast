{
  if (!hasNextCalled) {
    boolean hasNext=hasNext();
    if (!hasNext) {
      return null;
    }
  }
  if (next != null) {
    hasNextCalled=false;
  }
  if (type == TYPE_ENTRIES)   return next;
 else   if (type == TYPE_KEYS)   return next.getKey();
 else   return next.getValue();
}
