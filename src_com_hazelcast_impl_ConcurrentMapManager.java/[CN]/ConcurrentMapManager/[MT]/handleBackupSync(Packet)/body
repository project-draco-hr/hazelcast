{
  remoteReq.setFromPacket(packet);
  doBackup(remoteReq);
  if (remoteReq.response == Boolean.TRUE) {
    sendResponse(packet);
  }
 else {
    sendResponseFailure(packet);
  }
  remoteReq.reset();
}
