{
  Address owner=block.migrationAddress;
  if (owner == null)   owner=block.owner;
  if (owner == null)   return false;
  MemberImpl next=getNextMemberAfter(owner,true,1);
  return (next != null && thisAddress.equals(next.getAddress()));
}
