{
  if (inv.key != null && !rightRemoteTarget(inv))   return;
  remoteReq.setInvocation(inv);
  doContains(remoteReq);
  if (remoteReq.response == Boolean.TRUE) {
    sendResponse(inv);
  }
 else {
    sendResponseFailure(inv);
  }
  remoteReq.reset();
}
