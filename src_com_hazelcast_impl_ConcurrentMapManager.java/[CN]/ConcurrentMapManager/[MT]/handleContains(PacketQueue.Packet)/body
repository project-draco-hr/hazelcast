{
  if (packet.key != null && !rightRemoteTarget(packet))   return;
  remoteReq.setPacket(packet);
  doContains(remoteReq);
  if (remoteReq.response == Boolean.TRUE) {
    sendResponse(packet);
  }
 else {
    sendResponseFailure(packet);
  }
  remoteReq.reset();
}
