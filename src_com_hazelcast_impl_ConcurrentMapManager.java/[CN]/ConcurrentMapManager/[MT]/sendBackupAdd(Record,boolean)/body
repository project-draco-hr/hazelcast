{
  int min=3;
  if (force)   min=2;
  if (lsMembers.size() < min)   return;
  Invocation inv=toInvocation(record,true);
  inv.operation=OP_CMAP_BACKUP_ADD;
  boolean sent=send(inv,getNextMember().getAddress());
  if (!sent) {
    inv.returnToContainer();
  }
}
