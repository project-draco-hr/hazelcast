{
  if (lsMembers.size() < 3)   return;
  Invocation inv=toInvocation(record,false);
  inv.operation=OP_CMAP_BACKUP_LOCK;
  boolean sent=send(inv,getNextMemberAfter(thisAddress,true,1).getAddress());
  if (!sent) {
    inv.returnToContainer();
  }
}
