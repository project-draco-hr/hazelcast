{
  if (containsKey && !rightRemoteTarget(inv))   return;
  remoteReq.setInvocation(inv);
  doContains(containsKey,remoteReq);
  if (remoteReq.response == Boolean.TRUE) {
    sendResponse(inv);
  }
 else {
    sendResponseFailure(inv);
  }
  remoteReq.reset();
}
