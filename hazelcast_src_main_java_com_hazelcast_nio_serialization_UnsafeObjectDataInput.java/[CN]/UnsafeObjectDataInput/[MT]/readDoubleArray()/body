{
  int len=readInt();
  if (len > 0) {
    double[] values=new double[len];
    unsafeMemCopy(values,UnsafeHelper.DOUBLE_ARRAY_BASE_OFFSET,len << 3);
    return values;
  }
  return new double[0];
}
