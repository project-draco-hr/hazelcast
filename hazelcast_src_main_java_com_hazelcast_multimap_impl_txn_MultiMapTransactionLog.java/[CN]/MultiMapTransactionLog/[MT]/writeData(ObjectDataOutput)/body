{
  out.writeUTF(name);
  out.writeInt(opList.size());
  for (  Operation op : opList) {
    out.writeObject(op);
  }
  key.writeData(out);
  out.writeLong(ttl);
  out.writeLong(threadId);
  out.writeLong(txVersion);
}
