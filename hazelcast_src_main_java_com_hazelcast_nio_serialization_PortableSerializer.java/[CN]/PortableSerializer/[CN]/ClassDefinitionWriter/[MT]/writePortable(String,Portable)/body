{
  final FieldDefinitionImpl fd=new FieldDefinitionImpl(index++,fieldName,FieldDefinitionImpl.TYPE_PORTABLE,portable.getClassId());
  cd.add(fd);
  ClassDefinitionImpl nestedCd=getClassDefinition(portable);
  cd.add(nestedCd);
}
