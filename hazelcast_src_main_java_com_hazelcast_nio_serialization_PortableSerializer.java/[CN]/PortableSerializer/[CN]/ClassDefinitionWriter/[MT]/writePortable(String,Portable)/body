{
  if (portable == null) {
    throw new HazelcastSerializationException("Cannot write null portable without explicitly " + "registering class definition!");
  }
  ClassDefinitionBuilderImpl nestedBuilder=(ClassDefinitionBuilderImpl)builder.createPortableFieldBuilder(fieldName,portable.getClassId());
  addNestedField(portable,nestedBuilder);
}
