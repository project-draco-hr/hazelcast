{
  ClassDefinitionBuilderImpl nestedBuilder=(ClassDefinitionBuilderImpl)builder.createPortableFieldBuilder(fieldName,classId);
  if (portable != null) {
    addNestedField(portable,nestedBuilder);
  }
}
