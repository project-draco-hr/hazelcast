{
  ListenerAdapter listenerAdaptor=createMapListenerAdaptor(mapListener);
  EventRegistration registration=nodeEngine.getEventService().registerLocalListener(mapServiceContext.serviceName(),mapName,listenerAdaptor);
  return registration.getId();
}
