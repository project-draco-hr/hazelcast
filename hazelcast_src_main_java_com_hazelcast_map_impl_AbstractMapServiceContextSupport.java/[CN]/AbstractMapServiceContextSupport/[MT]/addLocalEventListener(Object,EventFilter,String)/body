{
  ListenerAdapter listenerAdaptor=createListenerAdapter(listener);
  EventRegistration registration=nodeEngine.getEventService().registerLocalListener(serviceName(),mapName,eventFilter,listenerAdaptor);
  return registration.getId();
}
