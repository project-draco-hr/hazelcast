{
  ListenerAdapter listenerAdaptor=createMapListenerAdaptor(mapListener);
  EventRegistration registration=nodeEngine.getEventService().registerListener(mapServiceContext.serviceName(),mapName,eventFilter,listenerAdaptor);
  return registration.getId();
}
