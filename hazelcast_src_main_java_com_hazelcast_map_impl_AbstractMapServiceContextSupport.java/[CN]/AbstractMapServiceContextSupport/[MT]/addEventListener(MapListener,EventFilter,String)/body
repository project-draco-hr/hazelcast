{
  ListenerAdapter listenerAdaptor=createMapListenerAdaptor(mapListener);
  EventRegistration registration=nodeEngine.getEventService().registerListener(serviceName(),mapName,eventFilter,listenerAdaptor);
  return registration.getId();
}
