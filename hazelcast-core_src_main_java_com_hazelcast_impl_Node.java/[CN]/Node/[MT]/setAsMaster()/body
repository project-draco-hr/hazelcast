{
  masterAddress=address;
  if (DEBUG)   logger.log(Level.FINEST,"adding member myself");
  ClusterManager.get().addMember(address,getLocalNodeType());
  clusterImpl.setMembers(BaseManager.lsMembers);
  unlock();
}
