{
  if (endpoint.isAlive()) {
    final EntryEventType type=event.getEventType();
    final String uuid=event.getMember().getUuid();
    sendClientMessage(null,encodeEvent(DefaultData.NULL_DATA,DefaultData.NULL_DATA,type.getType(),uuid,event.getNumberOfEntriesAffected()));
  }
}
