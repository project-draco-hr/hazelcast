{
  if (endpoint.isAlive()) {
    final EntryEventType type=event.getEventType();
    final String uuid=event.getMember().getUuid();
    ClientMessage entryEvent=AddEntryListenerEventParameters.encode(DefaultData.NULL_DATA,DefaultData.NULL_DATA,DefaultData.NULL_DATA,type.getType(),uuid,event.getNumberOfEntriesAffected());
    sendClientMessage(entryEvent);
  }
}
