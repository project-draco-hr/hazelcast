{
  ClientMessage request=XACollectTransactionsParameters.encode();
  ClientMessage response=invoke(request);
  DataCollectionResultParameters resultParameters=DataCollectionResultParameters.decode(response);
  SerializableXID[] xidArray=new SerializableXID[resultParameters.result.size()];
  SerializationService serializationService=getContext().getSerializationService();
  int index=0;
  for (  Data xidData : resultParameters.result) {
    xidArray[index++]=serializationService.toObject(xidData);
  }
  return xidArray;
}
