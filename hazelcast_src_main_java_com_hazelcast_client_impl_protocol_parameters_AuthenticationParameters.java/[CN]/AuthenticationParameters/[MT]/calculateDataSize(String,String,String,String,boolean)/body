{
  return ClientMessage.HEADER_SIZE + ParameterUtil.calculateStringDataSize(username) + ParameterUtil.calculateStringDataSize(password)+ ParameterUtil.calculateStringDataSize(uuid)+ ParameterUtil.calculateStringDataSize(ownerUuid)+ BitUtil.SIZE_OF_BYTE;
}
