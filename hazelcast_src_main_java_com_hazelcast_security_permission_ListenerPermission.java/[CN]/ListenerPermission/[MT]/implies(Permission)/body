{
  if (this.getClass() != permission.getClass()) {
    return false;
  }
  InstancePermission that=(InstancePermission)permission;
  if ("all".equals(that.getName()) || that.getName().equals(this.getName())) {
    return true;
  }
  return false;
}
