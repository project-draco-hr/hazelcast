{
  while (inBuffer.hasRemaining()) {
    if (packet == null) {
      packet=obtainPacket();
    }
    boolean complete=packet.readFrom(inBuffer);
    if (complete) {
      handlePacket(packet);
      packet=null;
    }
 else {
      break;
    }
  }
}
