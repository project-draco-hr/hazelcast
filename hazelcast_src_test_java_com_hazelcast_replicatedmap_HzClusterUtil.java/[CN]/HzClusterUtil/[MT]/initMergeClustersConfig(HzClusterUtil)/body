{
  String name;
  if (AUTO_RENAME) {
    name=this.getName() + "+" + b.getName();
  }
 else {
    name=this.getName();
  }
  this.setName(name);
  b.setName(name);
  List<String> all=this.getClusterEndPoints();
  all.addAll(b.getClusterEndPoints());
  this.setMembers(all);
  b.setMembers(all);
}
