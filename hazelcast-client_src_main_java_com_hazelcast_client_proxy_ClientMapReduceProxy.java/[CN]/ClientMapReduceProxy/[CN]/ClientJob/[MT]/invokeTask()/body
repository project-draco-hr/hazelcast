{
  ClientContext context=getContext();
  ClientInvocationService cis=context.getInvocationService();
  ClientMapReduceRequest request=new ClientMapReduceRequest(name,new ArrayList(keys),predicate,mapper,combinerFactory,reducerFactory,keyValueSource,chunkSize);
  cis.invokeOnRandomTarget(request);
}
