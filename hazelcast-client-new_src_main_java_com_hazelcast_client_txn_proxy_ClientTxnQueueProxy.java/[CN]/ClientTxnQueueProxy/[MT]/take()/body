{
  ClientMessage request=TransactionalQueueTakeParameters.encode(getName(),getTransactionId(),ThreadUtil.getThreadId());
  ClientMessage response=invoke(request);
  GenericResultParameters result=GenericResultParameters.decode(response);
  return (E)toObject(result.result);
}
