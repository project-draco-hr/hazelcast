{
  this.op=(Operation)waitSupport;
  this.waitSupport=waitSupport;
  this.queue=queue;
  this.expirationTime=waitSupport.getWaitTimeout() < 0 ? -1 : Clock.currentTimeMillis() + waitSupport.getWaitTimeout();
  this.setPartitionId(op.getPartitionId());
}
