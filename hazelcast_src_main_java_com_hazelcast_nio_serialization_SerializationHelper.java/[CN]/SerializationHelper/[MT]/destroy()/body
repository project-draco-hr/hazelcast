{
synchronized (bufferPool) {
    for (int i=0; i < bufferPool.length; i++) {
      SerializationBuffer buffer=bufferPool[i];
      if (buffer != null) {
        buffer.destroy();
        bufferPool[i]=null;
      }
    }
  }
}
