{
  if (is4BytesAligned(offset)) {
    super.putOrderedInt(o,offset,x);
  }
 else {
    throw new IllegalArgumentException("Unaligned memory accesses are not supported for ordered writes. " + "Offset must be 4-bytes aligned for integer typed ordered write, but it is " + offset);
  }
}
