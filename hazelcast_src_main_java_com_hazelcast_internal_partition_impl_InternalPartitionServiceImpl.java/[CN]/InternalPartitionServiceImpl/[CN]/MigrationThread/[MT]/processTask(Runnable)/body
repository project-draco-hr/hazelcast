{
  try {
    if (runnable == null || isInterrupted()) {
      return false;
    }
    runnable.run();
  }
 catch (  Throwable t) {
    logger.warning(t);
  }
 finally {
    migrationQueue.afterTaskCompletion(runnable);
  }
  return true;
}
