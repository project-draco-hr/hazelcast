{
  int size=in.readInt();
  if (size > 0)   entrySet=new HashSet<Map.Entry<Data,Data>>();
  for (int i=0; i < size; i++) {
    Map.Entry entry=new AbstractMap.SimpleImmutableEntry<Data,Data>(in.readData(),in.readData());
    entrySet.add(entry);
  }
}
