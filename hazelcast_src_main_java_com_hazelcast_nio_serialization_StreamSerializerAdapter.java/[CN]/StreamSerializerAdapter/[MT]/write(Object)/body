{
  final BufferObjectDataOutput out=service.pop();
  byte[] bytes;
  try {
    serializer.write(out,object);
    bytes=out.toByteArray();
  }
  finally {
    service.push(out);
  }
  return bytes;
}
