{
  target=getBackupMember(request.blockId,distance);
  if (target == null) {
    setResult(Boolean.FALSE);
  }
 else {
    if (target.equals(thisAddress)) {
      doLocalOp();
    }
 else {
      invoke();
    }
  }
}
