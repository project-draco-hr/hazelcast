{
  CMap cmap=getOrCreateMap(request.name);
  SystemLogService css=node.getCallStateService();
  if (css.shouldLog(CS_TRACE)) {
    css.logObject(request,CS_TRACE,"Calling cmap.put");
  }
  cmap.put(request);
  if (request.operation == CONCURRENT_MAP_TRY_PUT || request.operation == CONCURRENT_MAP_PUT_AND_UNLOCK) {
    request.response=Boolean.TRUE;
  }
  if (css.shouldLog(CS_INFO)) {
    css.info(request,"req.response",request.response);
  }
}
