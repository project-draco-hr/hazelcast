{
  long cost=0;
  if (getValue() != null) {
    cost=getValue().size();
    if (copyCount > 0) {
      cost*=copyCount;
    }
  }
 else   if (lsValues != null) {
    for (    Data data : lsValues) {
      cost+=data.size();
    }
  }
  return cost + getKey().size();
}
