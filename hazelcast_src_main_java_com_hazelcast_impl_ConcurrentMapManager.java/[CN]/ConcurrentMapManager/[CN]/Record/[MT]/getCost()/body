{
  long cost=0;
  if (value != null) {
    cost=value.size();
    if (copyCount > 0) {
      cost*=copyCount;
    }
  }
 else   if (lsValues != null) {
    for (    Data data : lsValues) {
      cost+=data.size();
    }
  }
  return cost + key.size();
}
