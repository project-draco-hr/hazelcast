{
  StringBuilder sb=new StringBuilder("======== BLOCKS =========");
  for (int i=0; i < BLOCK_COUNT; i++) {
    sb.append("\n\t");
    sb.append(blocks[i]);
  }
  sb.append("\n==============================");
  return sb.toString();
}
