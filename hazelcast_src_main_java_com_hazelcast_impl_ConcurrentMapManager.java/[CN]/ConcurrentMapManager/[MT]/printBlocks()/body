{
  logger.log(Level.FINEST,"=========================================");
  for (int i=0; i < BLOCK_COUNT; i++) {
    logger.log(Level.FINEST,String.valueOf(blocks[i]));
  }
  logger.log(Level.FINEST,"=========================================");
}
