{
  for (  CMap cmap : maps.values()) {
    try {
      flush(cmap.name);
      cmap.reset();
    }
 catch (    Throwable e) {
      if (node.isActive()) {
        logger.log(Level.SEVERE,e.getMessage(),e);
      }
    }
  }
}
