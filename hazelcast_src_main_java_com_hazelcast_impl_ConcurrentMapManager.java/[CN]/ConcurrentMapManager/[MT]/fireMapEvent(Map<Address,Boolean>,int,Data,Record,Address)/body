{
  if (record.getListeners() == null && (mapListeners == null || mapListeners.size() == 0)) {
    return;
  }
  fireMapEvent(mapListeners,record.getName(),eventType,record.getKeyData(),oldValue,record.getValueData(),record.getListeners(),callerAddress);
}
