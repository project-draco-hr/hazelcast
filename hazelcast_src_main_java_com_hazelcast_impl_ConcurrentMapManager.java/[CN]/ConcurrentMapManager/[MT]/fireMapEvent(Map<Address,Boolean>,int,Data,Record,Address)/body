{
  if (record.getListeners() == null && (mapListeners == null || mapListeners.size() == 0)) {
    return;
  }
  checkServiceThread();
  fireMapEvent(mapListeners,record.getName(),eventType,record.getKey(),oldValue,record.getValue(),record.getListeners(),callerAddress);
}
