{
  executeLocally(new Runnable(){
    public void run(){
      try {
        MEvict mEvict=new MEvict();
        mEvict.evict(name,key);
      }
 catch (      Exception ignored) {
      }
    }
  }
);
}
