{
  MLock mlock=new MLock();
  try {
    return mlock.lock(name,key,timeout);
  }
 catch (  OperationTimeoutException e) {
    if (timeout < 0) {
      throw e;
    }
 else {
      return false;
    }
  }
}
