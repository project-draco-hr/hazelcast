{
  enqueueAndWait(new Processable(){
    public void process(){
      getOrCreateMap(name);
    }
  }
,10);
  CMap cmap=getMap(name);
  if (cmap.loader != null) {
    if (!cmap.isMapForQueue()) {
    }
  }
}
