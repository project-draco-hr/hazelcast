{
  checkServiceThread();
  CMap map=maps.get(name);
  if (map == null) {
    map=new CMap(this,name);
    CMap anotherMap=maps.putIfAbsent(name,map);
    if (anotherMap != null) {
      map=anotherMap;
    }
  }
  return map;
}
