{
  return ClientMessage.HEADER_SIZE + ParameterUtil.calculateStringDataSize(name) + ParameterUtil.calculateByteArrayDataSize(key)+ ParameterUtil.calculateByteArrayDataSize(value)+ BitUtil.SIZE_OF_LONG+ BitUtil.SIZE_OF_LONG+ BitUtil.SIZE_OF_BYTE;
}
