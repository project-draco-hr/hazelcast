{
  if (response != null && response.buffers != null && response.hasBuffer()) {
    return client.getSerializationService().toObject(new Data(SerializationConstants.CONSTANT_TYPE_BYTE_ARRAY,response.buffers[0].array()));
  }
 else   return null;
}
