{
  byte[] k=null;
  byte[] v=null;
  if (key != null) {
    k=toByte(key);
  }
  if (value != null) {
    v=toByte(value);
  }
  Packet request=createRequestPacket(operation,k,v);
  return request;
}
