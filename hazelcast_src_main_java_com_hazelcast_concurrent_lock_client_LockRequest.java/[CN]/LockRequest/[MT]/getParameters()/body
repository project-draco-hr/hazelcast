{
  if (timeout == -1 && ttl != -1L) {
    return new Object[]{ttl,TimeUnit.MILLISECONDS};
  }
 else   if (timeout >= 0) {
    return new Object[]{timeout,TimeUnit.MILLISECONDS};
  }
  return null;
}
