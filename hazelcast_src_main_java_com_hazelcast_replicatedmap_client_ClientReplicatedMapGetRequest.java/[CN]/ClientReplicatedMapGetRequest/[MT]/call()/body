{
  ReplicatedRecordStore recordStore=getReplicatedRecordStore();
  ReplicatedRecord record=recordStore.getReplicatedRecord(key);
  return new ReplicatedMapGetResponse(recordStore.unmarshallValue(record.getValue()),record.getTtlMillis(),record.getUpdateTime());
}
