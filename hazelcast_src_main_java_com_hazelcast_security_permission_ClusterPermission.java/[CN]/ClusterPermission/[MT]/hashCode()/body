{
  if (hashcode == 0) {
    final int prime=31;
    int result=1;
    if (getName() == null) {
      result=prime * result + 13;
    }
 else {
      result=prime * result + getName().hashCode();
    }
    hashcode=result;
  }
  return hashcode;
}
