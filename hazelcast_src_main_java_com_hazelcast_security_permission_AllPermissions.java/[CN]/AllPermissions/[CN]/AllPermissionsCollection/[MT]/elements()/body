{
  return new Enumeration<Permission>(){
    boolean more=all;
    @Override public boolean hasMoreElements(){
      return more;
    }
    @Override public Permission nextElement(){
      more=false;
      return ALL_PERMISSIONS;
    }
  }
;
}
