{
  byte[] _indexTypes=indexTypes;
  sbState.append("\nIndex- records: " + records.size() + ", mapIndexes:"+ mapIndexes.size()+ ", indexTypes:"+ ((_indexTypes == null) ? 0 : _indexTypes.length));
  for (  Index index : mapIndexes.values()) {
    index.appendState(sbState);
  }
}
