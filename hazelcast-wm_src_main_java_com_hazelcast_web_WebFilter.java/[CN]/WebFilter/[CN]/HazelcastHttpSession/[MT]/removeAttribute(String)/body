{
  if (cacheLocally) {
    LocalCacheEntry entry=localCache.get(name);
    if (entry != null) {
      entry.value=null;
      entry.removed=true;
      entry.dirty=true;
    }
  }
 else {
    getClusterMap().delete(buildAttributeName(name));
  }
}
