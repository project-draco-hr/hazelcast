{
  List<Data> keys=clientMessage.getDataList();
  List<Data> values=clientMessage.getDataList();
  map=new HashMap<Data,Data>();
  for (int i=0; i < keys.size(); i++) {
    map.put(keys.get(i),values.get(i));
  }
}
