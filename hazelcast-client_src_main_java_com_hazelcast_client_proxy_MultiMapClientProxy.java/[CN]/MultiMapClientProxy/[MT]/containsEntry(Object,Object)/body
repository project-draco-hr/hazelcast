{
  check(key);
  check(value);
  return proxyHelper.doCommandAsBoolean(null,Command.MMCONTAINSENTRY,new String[]{getName()},client.getSerializationService().toData(key),client.getSerializationService().toData(value));
}
