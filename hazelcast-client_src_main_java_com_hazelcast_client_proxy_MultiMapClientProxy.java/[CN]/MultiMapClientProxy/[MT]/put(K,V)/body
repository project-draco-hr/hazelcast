{
  check(key);
  check(value);
  return proxyHelper.doCommandAsBoolean(Command.MMPUT,new String[]{getName()},client.getSerializationService().toData(key),client.getSerializationService().toData(value));
}
