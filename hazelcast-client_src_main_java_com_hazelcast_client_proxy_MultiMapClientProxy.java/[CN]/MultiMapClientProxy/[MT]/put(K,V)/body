{
  check(key);
  check(value);
  return proxyHelper.doCommandAsBoolean(null,Command.MMPUT,new String[]{getName()},client.getSerializationService().toData(key),client.getSerializationService().toData(value));
}
