{
  check(key);
  check(value);
  return proxyHelper.doCommandAsBoolean(Command.MMREMOVE,new String[]{getName()},client.getSerializationService().toData(key),client.getSerializationService().toData(value));
}
