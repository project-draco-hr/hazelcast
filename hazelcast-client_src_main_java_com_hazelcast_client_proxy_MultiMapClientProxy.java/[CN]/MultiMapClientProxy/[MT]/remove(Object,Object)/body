{
  check(key);
  check(value);
  return proxyHelper.doCommandAsBoolean(null,Command.MMREMOVE,new String[]{getName()},client.getSerializationService().toData(key),client.getSerializationService().toData(value));
}
