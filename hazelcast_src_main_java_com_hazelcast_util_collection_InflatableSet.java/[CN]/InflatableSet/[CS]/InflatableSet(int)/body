{
  this.state=State.INITIAL_LOAD;
  if (initialCapacity < 0) {
    throw new IllegalArgumentException("Initial Capacity must be a positive number. Current capacity: " + initialCapacity);
  }
  compactList=new ArrayList<T>(initialCapacity);
}
