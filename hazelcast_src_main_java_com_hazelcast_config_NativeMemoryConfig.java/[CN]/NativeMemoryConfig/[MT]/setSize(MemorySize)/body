{
  ValidationUtil.isNotNull(size,"Memory size");
  if (size.megaBytes() < MIN_INITIAL_MEMORY_SIZE) {
    throw new IllegalArgumentException("Initiali memory size cannot be less than " + MIN_INITIAL_MEMORY_SIZE + " MB");
  }
  this.size=size;
  return this;
}
