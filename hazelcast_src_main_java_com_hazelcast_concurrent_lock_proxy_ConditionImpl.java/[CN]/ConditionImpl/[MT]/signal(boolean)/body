{
  final NodeEngine nodeEngine=lockProxy.getNodeEngine();
  final Invocation inv=nodeEngine.getOperationService().createInvocationBuilder(SERVICE_NAME,new SignalOperation(namespace,lockProxy.key,ThreadUtil.getThreadId(),conditionId,all),partitionId).build();
  Future f=inv.invoke();
  try {
    f.get();
  }
 catch (  Throwable t) {
    throw ExceptionUtil.rethrow(t);
  }
}
