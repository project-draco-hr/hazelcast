{
  final Executor executor=packet.isHeaderSet(Packet.HEADER_EVENT) ? nodeEngine.executionService.eventExecutorService : nodeEngine.executionService.cachedExecutorService;
  executor.execute(new RemoteOperationExecutor(packet));
}
