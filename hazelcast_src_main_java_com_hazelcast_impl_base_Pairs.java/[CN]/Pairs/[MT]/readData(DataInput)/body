{
  int size=in.readInt();
  for (int i=0; i < size; i++) {
    if (getKeyValues() == null) {
      setKeyValues(new ArrayList<KeyValue>());
    }
    KeyValue kv=new KeyValue();
    kv.readData(in);
    getKeyValues().add(kv);
  }
}
