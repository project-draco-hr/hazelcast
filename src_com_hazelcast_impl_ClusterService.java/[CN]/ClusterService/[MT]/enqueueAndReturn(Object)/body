{
  try {
    if (queue.size() > 600)     if (DEBUG)     System.out.println("queue size " + queue.size());
    queue.put(message);
  }
 catch (  InterruptedException e) {
    Node.get().handleInterruptedException(Thread.currentThread(),e);
  }
}
