{
  try {
    if (queue.size() > 200)     if (DEBUG)     System.out.println("queue size " + queue.size());
    queue.put(message);
  }
 catch (  InterruptedException e) {
    Node.get().handleInterruptedException(Thread.currentThread(),e);
  }
}
