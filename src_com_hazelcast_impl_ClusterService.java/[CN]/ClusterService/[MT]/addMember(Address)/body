{
  if (address == null) {
    if (DEBUG) {
      System.out.println("Address cannot be null");
      return;
    }
  }
  MemberImpl member=getMember(address);
  if (member == null)   member=createMember(address);
  addMember(member);
}
