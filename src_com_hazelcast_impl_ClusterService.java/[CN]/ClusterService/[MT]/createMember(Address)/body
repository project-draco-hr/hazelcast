{
  MemberImpl member=new MemberImpl(address);
  if (address == thisAddress || address.equals(thisAddress)) {
    member.setThisMember(true);
    thisMember=member;
  }
 else   member.setThisMember(false);
  return member;
}
