{
  while (running) {
    try {
      process(queue.take());
    }
 catch (    InterruptedException e) {
      Node.get().handleInterruptedException(Thread.currentThread(),e);
    }
catch (    Exception e) {
      e.printStackTrace();
    }
  }
}
