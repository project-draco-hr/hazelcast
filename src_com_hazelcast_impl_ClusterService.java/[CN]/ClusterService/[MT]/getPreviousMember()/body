{
  if (lsMembers.size() < 2)   return null;
 else {
    if (thisMemberIndex == 0)     return lsMembers.get(lsMembers.size() - 1);
 else     return lsMembers.get((thisMemberIndex - 1) % lsMembers.size());
  }
}
