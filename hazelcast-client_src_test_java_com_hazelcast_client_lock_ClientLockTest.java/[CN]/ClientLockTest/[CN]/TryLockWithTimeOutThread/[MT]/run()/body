{
  for (int i=0; i < 1000 * 10; i++) {
    try {
      if (lock.tryLock(10,TimeUnit.MILLISECONDS)) {
        work();
        lock.unlock();
      }
    }
 catch (    InterruptedException e) {
      e.printStackTrace();
    }
  }
}
