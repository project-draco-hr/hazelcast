{
  name=in.readUTF();
  boolean isNullKey=in.readBoolean();
  if (!isNullKey) {
    key=new Data();
    key.readData(in);
  }
  threadId=in.readLong();
  ownerUuid=in.readUTF();
  op=in.readObject();
}
