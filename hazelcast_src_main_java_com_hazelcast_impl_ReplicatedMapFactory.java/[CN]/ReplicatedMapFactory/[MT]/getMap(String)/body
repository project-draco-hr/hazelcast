{
  IMap map=maps.get(name);
  if (map == null) {
synchronized (factoryLock) {
      map=maps.get(name);
      if (map == null) {
        map=new ReplicatedMap(name);
      }
    }
  }
  return map;
}
