{
  if (hash == Integer.MIN_VALUE) {
    int h=1;
    for (    ByteBuffer bb : lsData) {
      int limit=bb.limit();
      byte[] buffer=bb.array();
      for (int i=0; i < limit; i++) {
        h=31 * h + buffer[i];
      }
    }
    hash=h;
  }
  return hash;
}
