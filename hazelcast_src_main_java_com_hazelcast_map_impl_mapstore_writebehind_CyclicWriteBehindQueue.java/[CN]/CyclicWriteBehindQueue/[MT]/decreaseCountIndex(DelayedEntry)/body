{
  Data key=(Data)entry.getKey();
  Map<Data,Integer> index=this.index;
  Integer count=index.get(key);
  if (count == null) {
    return;
  }
  count--;
  if (count == 0) {
    index.remove(key);
  }
 else {
    index.put(key,count);
  }
}
