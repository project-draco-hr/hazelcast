{
  final Predicate predicate=query != null && query.trim().length() > 0 ? new SqlPredicate(query) : null;
  final Collection values=predicate != null ? getManagedObject().values(predicate) : getManagedObject().values();
  final List list=new ArrayList(values);
  return list.toString();
}
