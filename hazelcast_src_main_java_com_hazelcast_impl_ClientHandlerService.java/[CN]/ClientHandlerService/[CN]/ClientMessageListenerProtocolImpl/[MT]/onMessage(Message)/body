{
  DataMessage dm=(DataMessage)msg;
  Protocol p=new Protocol(clientEndpoint.conn,Command.MESSAGE,"-1",false,new String[]{name},ByteBuffer.wrap(dm.getMessageData().buffer));
  clientEndpoint.sendPacket(p);
}
