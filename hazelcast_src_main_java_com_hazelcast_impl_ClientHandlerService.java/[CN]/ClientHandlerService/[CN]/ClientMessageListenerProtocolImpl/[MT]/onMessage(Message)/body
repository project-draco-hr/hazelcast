{
  DataMessage dm=(DataMessage)msg;
  System.out.println("On message is called" + ((DataMessage)msg).getMessageData());
  Protocol p=new Protocol(clientEndpoint.conn,Command.MESSAGE.value,"-1",false,new String[]{name},ByteBuffer.wrap(dm.getMessageData().buffer));
  clientEndpoint.sendPacket(p);
}
