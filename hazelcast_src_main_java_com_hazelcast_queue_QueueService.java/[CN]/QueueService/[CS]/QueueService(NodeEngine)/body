{
  this.nodeEngine=nodeEngine;
  logger=nodeEngine.getLogger(QueueService.class);
  queueEvictionScheduler=EntryTaskSchedulerFactory.newScheduler(nodeEngine.getExecutionService().getDefaultScheduledExecutor(),new QueueEvictionProcessor(nodeEngine,this),ScheduleType.POSTPONE);
}
