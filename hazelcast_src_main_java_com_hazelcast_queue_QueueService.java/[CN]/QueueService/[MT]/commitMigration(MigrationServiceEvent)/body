{
  logger.log(Level.FINEST,"commit " + event.getPartitionId());
  if (event.getMigrationEndpoint() == MigrationServiceEvent.MigrationEndpoint.SOURCE && event.getMigrationType() == MigrationServiceEvent.MigrationType.MOVE) {
    cleanMigrationData(event.getPartitionId());
  }
}
