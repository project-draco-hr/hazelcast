{
  if (key instanceof String) {
    return String.valueOf(key);
  }
 else {
    Data data=serializationService.toData(key,PARTITION_STRATEGY);
    byte[] buffer=data.getBuffer();
    return Arrays.toString(buffer);
  }
}
