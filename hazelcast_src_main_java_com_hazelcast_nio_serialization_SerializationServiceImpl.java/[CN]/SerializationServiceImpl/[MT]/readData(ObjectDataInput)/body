{
  try {
    boolean isNull=input.readBoolean();
    if (isNull) {
      return null;
    }
    return new DefaultData(input.readByteArray());
  }
 catch (  Throwable e) {
    throw handleException(e);
  }
}
