{
  for (  TypeSerializer serializer : typeMap.values()) {
    serializer.destroy();
  }
  typeMap.clear();
  idMap.clear();
  fallback.set(null);
  for (  ContextAwareDataOutput output : outputPool) {
    output.close();
  }
  outputPool.clear();
}
