{
  str=str.trim();
  if (str.startsWith("[")) {
    str=str.substring(1);
  }
  if (str.endsWith("]")) {
    str=str.substring(0,str.length() - 1);
  }
  String ret[]=str.split(delimiter);
  for (int i=0; i < ret.length; i++) {
    ret[i]=ret[i].trim();
  }
  return ret;
}
