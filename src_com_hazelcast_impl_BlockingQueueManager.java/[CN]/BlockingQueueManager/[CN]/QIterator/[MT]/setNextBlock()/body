{
  if (currentIndex == -1 || currentIndex >= Block.BLOCK_SIZE) {
    if (blocks.size() == 0) {
      return false;
    }
 else {
      currentBlockId=blocks.remove(0);
      currentIndex=0;
      return true;
    }
  }
  return true;
}
