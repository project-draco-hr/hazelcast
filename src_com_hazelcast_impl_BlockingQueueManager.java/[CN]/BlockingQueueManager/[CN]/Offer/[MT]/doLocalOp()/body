{
  if (request.operation == OP_B_OFFER) {
    doOffer(request);
    if (!request.scheduled) {
      setResult(request.response);
    }
  }
 else {
    doPublish(request);
    setResult(request.response);
  }
}
