{
  int size=lsBlocks.size();
  for (int i=0; i < size; i++) {
    Block block=lsBlocks.get(i);
    if (block.blockId == fullBlockId) {
      block.setFull(true);
      blCurrentPut=null;
      return;
    }
  }
}
