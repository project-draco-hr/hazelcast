{
  if (DEBUG) {
    log("====================");
    for (    Block block : lsBlocks) {
      log(block);
    }
    log("=====================");
    log("CurrenTake " + blCurrentTake);
    log("CurrentPut " + blCurrentPut);
    log("---------------------");
  }
}
