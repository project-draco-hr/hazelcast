{
  total+=(int)inv.longValue;
  inv.returnToContainer();
  numberOfResponses++;
  if (numberOfResponses >= numberOfExpectedResponses) {
    removeCall(getId());
synchronized (this) {
      notify();
    }
  }
}
