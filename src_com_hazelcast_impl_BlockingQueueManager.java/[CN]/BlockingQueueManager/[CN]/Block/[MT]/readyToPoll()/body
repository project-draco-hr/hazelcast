{
  for (int i=removeIndex; i < BLOCK_SIZE; i++) {
    if (values[i] != null) {
      Data value=values[i];
      long age=System.currentTimeMillis() - value.createDate;
      if (age > maxAge) {
        values[i]=null;
        size--;
      }
 else {
        return true;
      }
    }
  }
  return false;
}
