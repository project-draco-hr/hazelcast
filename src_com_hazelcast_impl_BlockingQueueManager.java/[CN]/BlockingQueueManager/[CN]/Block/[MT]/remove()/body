{
  for (int i=removeIndex; i < BLOCK_SIZE; i++) {
    if (values[i] != null) {
      Data value=values[i];
      values[i]=null;
      size--;
      removeIndex=i;
      return value;
    }
  }
  return null;
}
