{
  int index=BLOCK_SIZE - 1;
  while (index >= 0) {
    if (values[index] != null) {
      addIndex=index + 1;
      if (addIndex >= BLOCK_SIZE) {
        full=true;
      }
      return;
    }
    index--;
  }
  index=0;
}
