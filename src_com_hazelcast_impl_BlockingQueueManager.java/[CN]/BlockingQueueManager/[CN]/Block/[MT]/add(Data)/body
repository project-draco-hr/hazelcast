{
  if (values[addIndex] != null)   return -1;
  values[addIndex]=data;
  size++;
  int addedCurrentIndex=addIndex;
  addIndex++;
  if (addIndex >= BLOCK_SIZE) {
    full=true;
  }
  return addedCurrentIndex;
}
