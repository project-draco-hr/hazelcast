{
  int sum=0;
  for (  Map<String,List<Integer>> group : values) {
    for (    List<Integer> entries : group.values())     for (    Integer value : entries) {
      sum+=value;
    }
  }
  return sum;
}
