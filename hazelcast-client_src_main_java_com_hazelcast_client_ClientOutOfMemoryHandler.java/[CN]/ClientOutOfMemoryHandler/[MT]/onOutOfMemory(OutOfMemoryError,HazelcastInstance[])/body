{
  System.err.println(oom);
  for (  HazelcastInstance instance : hazelcastInstances) {
    if (instance instanceof HazelcastClientInstance) {
      ClientHelper.cleanResources((HazelcastClientInstance)instance);
    }
  }
}
