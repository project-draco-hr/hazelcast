{
  Object key=entry.getKey();
  if (key instanceof String) {
    String k=(String)key;
    if (k.startsWith(sessionIdWithAttributeSeparator)) {
      entry.setValue(null);
    }
  }
  return null;
}
