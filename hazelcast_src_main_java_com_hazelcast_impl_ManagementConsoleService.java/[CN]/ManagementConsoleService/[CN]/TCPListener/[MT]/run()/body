{
  try {
    while (running) {
      ClientHandler clientHandler=qBuffers.poll();
      serverSocket.doAccept(clientHandler.getSocket());
      clientHandler.start();
    }
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
}
