{
  RingbufferConfig config=new RingbufferConfig(NAME);
  String s=config.toString();
  assertEquals("RingbufferConfig{name='someringbuffer', capacity=10000, backupCount=1, " + "asyncBackupCount=0, timeToLiveSeconds=0, inMemoryFormat=BINARY}",s);
}
