{
  if (!mapContainer.isWriteBehindMapStoreEnabled()) {
    return;
  }
  final DelayedEntry<Data,Object> delayedEntry=mapService.constructDelayedEntry(dataKey,recordValue,partitionId,mapContainer);
  if (recordValue == null) {
    addToWriteBehindWaitingDeletions(dataKey);
  }
  writeBehindQueue.offer(delayedEntry);
}
