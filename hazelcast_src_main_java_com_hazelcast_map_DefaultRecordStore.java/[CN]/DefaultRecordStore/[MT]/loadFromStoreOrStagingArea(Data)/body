{
  if (hasWaitingWriteBehindDeleteOperation(key)) {
    return null;
  }
  final Object fromStagingArea=getFromEvictionStagingArea(key);
  return fromStagingArea == null ? mapContainer.getStore().load(mapService.toObject(key)) : fromStagingArea;
}
