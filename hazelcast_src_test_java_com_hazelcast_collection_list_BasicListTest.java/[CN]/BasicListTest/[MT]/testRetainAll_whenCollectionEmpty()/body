{
  IList list=newList();
  for (int i=0; i < 10; i++) {
    list.add(i,"item" + i);
  }
  List listTest=new ArrayList<String>();
  assertTrue(list.retainAll(listTest));
  assertSizeEventually(0,list);
}
