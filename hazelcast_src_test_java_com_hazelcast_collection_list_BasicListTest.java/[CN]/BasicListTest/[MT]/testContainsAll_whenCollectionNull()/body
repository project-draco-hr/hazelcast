{
  IList list=newList();
  for (int i=0; i < 10; i++) {
    list.add(i,"item" + i);
  }
  List listTest=null;
  try {
    assertFalse(list.containsAll(listTest));
    fail();
  }
 catch (  NullPointerException e) {
  }
}
