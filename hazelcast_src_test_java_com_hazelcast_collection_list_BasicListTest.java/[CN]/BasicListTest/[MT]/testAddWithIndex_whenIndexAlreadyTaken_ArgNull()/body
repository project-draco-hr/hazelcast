{
  IList list=newList();
  for (int i=0; i < 10; i++) {
    list.add(i,"item" + i);
  }
  assertEquals("item4",list.get(4));
  try {
    list.add(4,null);
    fail();
  }
 catch (  NullPointerException e) {
  }
  assertEquals("item4",list.get(4));
}
