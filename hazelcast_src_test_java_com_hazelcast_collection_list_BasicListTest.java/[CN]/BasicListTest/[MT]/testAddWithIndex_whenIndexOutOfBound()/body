{
  IList list=newList();
  for (int i=0; i < 10; i++) {
    list.add(i,"item" + i);
  }
  assertSizeEventually(10,list);
  try {
    list.add(14,"item14");
    fail();
  }
 catch (  IndexOutOfBoundsException e) {
  }
  assertEquals(10,list.size());
}
