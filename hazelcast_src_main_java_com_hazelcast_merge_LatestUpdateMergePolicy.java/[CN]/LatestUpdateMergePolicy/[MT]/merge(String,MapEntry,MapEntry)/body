{
  if (existingEntry == null || existingEntry.getValue() == null) {
    return mergingEntry.getValue();
  }
  return (mergingEntry.getLastUpdateTime() > existingEntry.getLastUpdateTime()) ? mergingEntry.getValue() : existingEntry.getValue();
}
