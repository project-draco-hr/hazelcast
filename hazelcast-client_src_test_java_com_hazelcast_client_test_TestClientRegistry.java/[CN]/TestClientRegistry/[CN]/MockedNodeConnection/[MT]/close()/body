{
  super.close();
  ClientConnectionManager connectionManager=responseConnection.getConnectionManager();
  connectionManager.destroyConnection(responseConnection,new TargetDisconnectedException("Mocked Remote socket closed"));
}
