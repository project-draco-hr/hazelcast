{
  ensureOpen();
  final NodeEngine nodeEngine=getNodeEngine();
  try {
    nodeEngine.getOperationService().invokeOnAllPartitions(getServiceName(),new CacheClearOperationFactory(name));
  }
 catch (  Throwable t) {
    throw ExceptionUtil.rethrow(t);
  }
}
