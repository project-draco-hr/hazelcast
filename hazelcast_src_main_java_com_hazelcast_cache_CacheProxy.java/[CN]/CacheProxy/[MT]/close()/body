{
  isClosed=true;
  delegate.destroy();
  if (cacheLoader instanceof Closeable) {
    try {
      ((Closeable)cacheLoader).close();
    }
 catch (    IOException e) {
    }
  }
  deregisterCompletionListener();
}
