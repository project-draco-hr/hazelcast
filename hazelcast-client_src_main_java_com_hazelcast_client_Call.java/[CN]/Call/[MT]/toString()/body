{
  return "Call " + "[" + id + "] operation="+ (request != null ? request.getOperation() + " " + request.getName()+ " "+ (response ? "+ " : "- ")+ Serializer.toObject(request.getKey())+ "="+ Serializer.toObject(request.getValue()) : null);
}
