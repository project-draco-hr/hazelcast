{
  EntryObject e=new PredicateBuilder().getEntryObject();
  Predicate predicate=e.is("active").and(e.get("salary").between(2000.22,4000.44)).and(e.get("age").between(10,90));
  testIndexPerformance(predicate);
}
