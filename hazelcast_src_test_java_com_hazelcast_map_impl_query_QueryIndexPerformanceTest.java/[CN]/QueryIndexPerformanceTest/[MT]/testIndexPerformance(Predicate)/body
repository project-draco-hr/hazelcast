{
  long tookWithoutIndex=runQuery(mapWithoutIndex,predicate);
  long tookWithIndex=runQuery(mapWithIndex,predicate);
  assertTrue("withIndex: " + tookWithIndex + " nanos, withoutIndex: "+ tookWithoutIndex+ " nanos",tookWithIndex < tookWithoutIndex);
}
