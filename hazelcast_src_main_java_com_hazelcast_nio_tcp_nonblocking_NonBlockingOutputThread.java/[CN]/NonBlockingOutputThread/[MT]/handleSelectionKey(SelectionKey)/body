{
  if (sk.isValid() && sk.isWritable()) {
    writeEvents.inc();
    SelectionHandler handler=(SelectionHandler)sk.attachment();
    try {
      handler.handle();
    }
 catch (    Throwable t) {
      handler.onFailure(t);
    }
  }
}
