{
  selectorQueue.clear();
  try {
    addTask(new Runnable(){
      @Override public void run(){
        running=false;
      }
    }
);
    interrupt();
  }
 catch (  Throwable t) {
    logger.finest("Exception while waiting for shutdown",t);
  }
}
