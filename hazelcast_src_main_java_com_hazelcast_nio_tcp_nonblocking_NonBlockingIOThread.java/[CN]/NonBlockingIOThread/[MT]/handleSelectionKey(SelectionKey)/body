{
  if (!sk.isValid()) {
    return;
  }
  eventCount.inc();
  SelectionHandler handler=(SelectionHandler)sk.attachment();
  try {
    handler.handle();
  }
 catch (  Throwable t) {
    handler.onFailure(t);
  }
}
