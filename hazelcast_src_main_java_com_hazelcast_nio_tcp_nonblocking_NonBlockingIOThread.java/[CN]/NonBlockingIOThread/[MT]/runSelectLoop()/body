{
  while (!isInterrupted()) {
    processTaskQueue();
    int selectedKeys=selector.select(SELECT_WAIT_TIME_MILLIS);
    lastSelectTimeMs=System.currentTimeMillis();
    if (selectedKeys > 0) {
      handleSelectionKeys();
    }
  }
}
