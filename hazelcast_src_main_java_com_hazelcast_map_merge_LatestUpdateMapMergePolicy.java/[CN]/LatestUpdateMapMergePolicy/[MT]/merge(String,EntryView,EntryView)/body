{
  if (mergingEntry.getLastUpdateTime() > existingEntry.getLastUpdateTime())   return mergingEntry.getValue();
  return existingEntry.getValue() == null ? mergingEntry.getValue() : existingEntry.getValue();
}
