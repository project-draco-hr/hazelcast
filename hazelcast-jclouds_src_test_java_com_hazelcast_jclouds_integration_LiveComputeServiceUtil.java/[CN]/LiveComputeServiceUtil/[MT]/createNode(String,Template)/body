{
  NodeMetadata node=getOnlyElement(computeService.createNodesInGroup(groupName,1,template));
  LOGGER.info(MessageFormat.format("<< node created {0}: {1}",node.getId(),concat(node.getPrivateAddresses(),node.getPublicAddresses())));
  nodes.add(node);
  return node;
}
