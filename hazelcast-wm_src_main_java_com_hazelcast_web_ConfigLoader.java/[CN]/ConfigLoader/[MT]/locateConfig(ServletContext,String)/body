{
  URL url=null;
  try {
    url=context.getResource(path);
  }
 catch (  MalformedURLException e) {
  }
  if (url == null) {
    url=asURL(path);
  }
  if (url == null) {
    url=asResource(path);
  }
  return url;
}
