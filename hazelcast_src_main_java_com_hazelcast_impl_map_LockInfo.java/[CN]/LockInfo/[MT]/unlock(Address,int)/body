{
  checkTTL();
  if (lockCount == 0) {
    return false;
  }
 else {
    if (isLockedBy(address,threadId)) {
      lockCount--;
      if (lockCount == 0) {
        clear();
      }
      return true;
    }
  }
  return false;
}
