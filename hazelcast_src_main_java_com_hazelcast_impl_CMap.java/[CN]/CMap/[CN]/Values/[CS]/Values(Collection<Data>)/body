{
  super();
  if (values != null) {
    this.lsValues=new ArrayList<Data>(values.size());
    for (    Data data : values) {
      if (data != null) {
        lsValues.add(data);
      }
    }
  }
}
