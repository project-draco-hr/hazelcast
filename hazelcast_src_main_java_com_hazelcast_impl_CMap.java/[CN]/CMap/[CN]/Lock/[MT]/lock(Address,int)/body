{
  if (this.lockCount == 0) {
    this.lockAddress=address;
    this.lockThreadId=threadId;
    this.lockCount++;
    return true;
  }
 else   if (isLockedBy(address,threadId)) {
    this.lockCount++;
    return true;
  }
  return false;
}
