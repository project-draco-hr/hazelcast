{
  if (result != null) {
    return getResult();
  }
  try {
    processResult(remoteCall.getResponse());
  }
 catch (  TimeoutException e) {
    throw new ExecutionException(e);
  }
  return getResult();
}
