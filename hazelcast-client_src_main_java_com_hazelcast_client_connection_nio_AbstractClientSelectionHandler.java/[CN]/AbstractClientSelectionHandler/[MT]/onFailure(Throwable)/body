{
  if (sk != null) {
    sk.cancel();
  }
  connectionManager.destroyConnection(connection);
  logger.warning(Thread.currentThread().getName() + " Closing socket to endpoint " + connection.getEndPoint()+ ", Cause:"+ e);
}
