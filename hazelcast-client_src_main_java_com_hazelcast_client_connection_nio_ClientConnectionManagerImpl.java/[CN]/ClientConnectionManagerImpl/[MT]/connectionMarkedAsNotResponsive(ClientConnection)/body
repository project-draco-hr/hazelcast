{
  if (smartRouting) {
    return;
  }
  try {
    ownerConnection.close();
  }
 catch (  Exception ignored) {
    EmptyStatement.ignore(ignored);
  }
  try {
    connection.close();
  }
 catch (  Exception ignored) {
    EmptyStatement.ignore(ignored);
  }
}
