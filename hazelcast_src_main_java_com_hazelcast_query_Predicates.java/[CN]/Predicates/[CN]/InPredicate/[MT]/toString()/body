{
  final StringBuffer sb=new StringBuffer();
  sb.append(first);
  sb.append(" IN (");
  for (int i=0; i < values.length; i++) {
    sb.append(values[i]);
    if (i < (values.length - 1)) {
      sb.append(",");
    }
  }
  sb.append(")");
  return sb.toString();
}
