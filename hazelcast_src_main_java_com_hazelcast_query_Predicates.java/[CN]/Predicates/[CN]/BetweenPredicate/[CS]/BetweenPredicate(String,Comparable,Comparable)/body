{
  super(first);
  if (from == null) {
    throw new IllegalArgumentException();
  }
 else   if (to == null) {
    throw new IllegalArgumentException();
  }
 else {
    this.from=from;
  }
  this.to=to;
}
