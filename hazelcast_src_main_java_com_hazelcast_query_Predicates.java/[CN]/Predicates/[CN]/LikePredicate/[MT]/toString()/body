{
  StringBuilder builder=new StringBuilder(attribute).append(" ").append((flags & Pattern.CASE_INSENSITIVE) == 0 ? "LIKE" : "ILIKE").append(" '").append(second).append("'");
  return builder.toString();
}
