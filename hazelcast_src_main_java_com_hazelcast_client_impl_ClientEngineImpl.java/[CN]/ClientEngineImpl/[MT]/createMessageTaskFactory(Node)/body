{
  try {
    Class<?> aClass=ClassLoaderUtil.loadClass(ClientEngineImpl.class.getClassLoader(),"com.hazelcast.client.impl.protocol.MessageTaskFactoryImpl");
    Constructor<?> constructor=aClass.getConstructor(new Class[]{Node.class});
    return (MessageTaskFactory)constructor.newInstance(new Object[]{node});
  }
 catch (  Exception e) {
    throw ExceptionUtil.rethrow(e);
  }
}
