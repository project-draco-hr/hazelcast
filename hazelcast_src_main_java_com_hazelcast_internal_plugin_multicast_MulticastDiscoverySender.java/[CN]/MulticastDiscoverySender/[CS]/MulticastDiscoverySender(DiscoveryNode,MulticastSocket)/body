{
  this.discoveryNode=discoveryNode;
  this.multicastSocket=multicastSocket;
  if (discoveryNode != null)   memberInfo=new MemberInfo(discoveryNode.getPublicAddress().getHost(),discoveryNode.getPublicAddress().getPort());
  initDatagramPacket();
}
