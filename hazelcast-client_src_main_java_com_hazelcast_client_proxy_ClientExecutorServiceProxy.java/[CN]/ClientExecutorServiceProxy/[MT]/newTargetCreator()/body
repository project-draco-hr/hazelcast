{
  return new ConstructorFunction<Object,Address>(){
    @Override public Address createNew(    Object arg){
      final int partitionId=randomPartitionId();
      return getPartitionOwner(partitionId);
    }
  }
;
}
