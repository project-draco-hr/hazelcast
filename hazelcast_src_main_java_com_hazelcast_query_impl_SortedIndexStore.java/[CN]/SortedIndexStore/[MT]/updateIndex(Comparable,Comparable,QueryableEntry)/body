{
  if (oldValue.equals(newValue)) {
    return;
  }
  ensureNotLocked();
  takeLock();
  try {
    removeIndex(oldValue,entry.getIndexKey());
    newIndex(newValue,entry);
  }
  finally {
    releaseLock();
  }
}
