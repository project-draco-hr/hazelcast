{
  if (ordered) {
    TreeSet<Long> ordered=new TreeSet<Long>();
    Set<Long> values=mapRecords.keySet();
    for (    Long value : values) {
      ordered.add(value);
    }
    valueOrder=ordered;
  }
}
