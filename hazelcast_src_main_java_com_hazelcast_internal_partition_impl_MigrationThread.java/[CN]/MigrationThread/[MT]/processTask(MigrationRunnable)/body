{
  try {
    if (runnable == null || isInterrupted()) {
      return false;
    }
    runnable.run();
  }
 catch (  Throwable t) {
    logger.warning(t);
  }
 finally {
    queue.afterTaskCompletion(runnable);
  }
  return true;
}
