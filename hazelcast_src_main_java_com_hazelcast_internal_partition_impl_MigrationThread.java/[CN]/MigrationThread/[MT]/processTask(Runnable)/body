{
  try {
    if (r == null || isInterrupted()) {
      return false;
    }
    r.run();
  }
 catch (  Throwable t) {
    logger.warning(t);
  }
 finally {
    queue.afterTaskCompletion(r);
  }
  return true;
}
