{
  if (toAddress == null) {
    toAddress=node.getMasterAddress();
  }
  JoinRequest joinRequest=node.createJoinInfo(withCredentials);
  Invocation inv=node.nodeService.createSingleInvocation(SERVICE_NAME,joinRequest,NodeService.EXECUTOR_THREAD_ID).setTarget(toAddress).setTryCount(1).build();
  inv.invoke();
  return true;
}
