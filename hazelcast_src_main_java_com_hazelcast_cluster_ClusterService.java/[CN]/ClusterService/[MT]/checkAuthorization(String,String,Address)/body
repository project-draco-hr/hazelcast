{
  Operation authorizationCall=new AuthorizationCall(groupName,groupPassword);
  Future<Boolean> future=node.nodeService.createSingleInvocation(SERVICE_NAME,authorizationCall,-1).setTarget(target).setTryCount(1).build().invoke();
  try {
    return future.get();
  }
 catch (  Exception ignored) {
  }
  return false;
}
