{
  lock.lock();
  try {
    if (setJoins != null) {
      setJoins.clear();
    }
    timeToStartJoin=0;
    membersRef.set(null);
    dataMemberCount.set(0);
    masterConfirmationTimes.clear();
  }
  finally {
    lock.unlock();
  }
}
