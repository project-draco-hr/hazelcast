{
  lock.lock();
  try {
    setJoins.clear();
    timeToStartJoin=0;
    membersRef.set(null);
    dataMemberCount.set(0);
    masterConfirmationTimes.clear();
  }
  finally {
    lock.unlock();
  }
}
