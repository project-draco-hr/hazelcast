{
  if (data != null && data.length > 0 && data.length < DATA_OFFSET) {
    throw new IllegalArgumentException("Data should be either empty or should contain more than " + HeapData.DATA_OFFSET + " bytes! -> "+ Arrays.toString(data));
  }
  this.data=data;
}
