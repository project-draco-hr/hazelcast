{
  Protocol protocol;
  if (predicate == null)   protocol=protocolProxyHelper.doCommand(Command.MENTRYSET,new String[]{getName()},null);
 else   protocol=protocolProxyHelper.doCommand(Command.MENTRYSET,new String[]{getName()},toData(predicate));
  int size=protocol.buffers == null ? 0 : protocol.buffers.length;
  Map<K,V> map=new HashMap<K,V>();
  for (int i=0; i < size; ) {
    K key=(K)toObject(new Data(protocol.buffers[i++].array()));
    V value=(V)toObject(new Data(protocol.buffers[i++].array()));
    map.put(key,value);
  }
  return map;
}
