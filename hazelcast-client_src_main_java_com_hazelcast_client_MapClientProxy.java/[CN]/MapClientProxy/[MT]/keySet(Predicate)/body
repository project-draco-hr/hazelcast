{
  Protocol protocol;
  if (predicate == null)   protocol=protocolProxyHelper.doCommand(Command.KEYSET,new String[]{"map",getName()},null);
 else   protocol=protocolProxyHelper.doCommand(Command.KEYSET,new String[]{"map",getName()},protocolProxyHelper.toData(predicate));
  if (!protocol.hasBuffer())   return Collections.emptySet();
  Set<K> set=new HashSet<K>(protocol.buffers.length);
  for (  ByteBuffer b : protocol.buffers) {
    set.add((K)protocolProxyHelper.toObject(new Data(SerializationConstants.CONSTANT_TYPE_BYTE_ARRAY,b.array())));
  }
  return set;
}
