{
  EventRegistration eventRegistration=eventService.registerListener(TopicService.NAME,name,listener);
  if (registeredIds.putIfAbsent(name,eventRegistration.getId()) != null)   System.out.println("Already registered");
}
