{
  EventRegistration eventRegistration=eventService.registerListener(TopicService.NAME,name,listener);
  if (registeredIds.putIfAbsent(name,eventRegistration.getId()) != null)   logger.log(Level.FINEST,"Topic:" + getName() + " is already registered as message listener");
}
