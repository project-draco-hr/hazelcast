{
  if (registeredIds.get(name) == null)   logger.log(Level.FINEST,"There is no registered topic with the name " + getName() + " ");
 else {
    eventService.deregisterListener(TopicService.NAME,name,registeredIds.get(name));
    registeredIds.remove(name);
  }
}
