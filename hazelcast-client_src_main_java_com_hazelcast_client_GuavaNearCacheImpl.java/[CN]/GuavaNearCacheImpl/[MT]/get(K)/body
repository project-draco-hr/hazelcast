{
  try {
    return cache.get(key).orNull();
  }
 catch (  ExecutionException e) {
    if (e.getCause() instanceof RuntimeException) {
      throw (RuntimeException)e.getCause();
    }
 else     return map.get0(key);
  }
}
