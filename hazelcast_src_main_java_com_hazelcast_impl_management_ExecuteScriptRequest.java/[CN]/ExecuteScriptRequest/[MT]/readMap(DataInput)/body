{
  int size=in.readInt();
  Map props=new HashMap(size);
  if (size > 0) {
    for (int i=0; i < size; i++) {
      Object key=readObject(in);
      Object value=readObject(in);
      props.put(key,value);
    }
  }
  return props;
}
