{
  initExecutor();
  assertEquals(groupProperties.getInteger(GroupProperty.PARTITION_COUNT),executor.getPartitionOperationRunners().length);
  assertEquals(executor.getGenericThreadCount(),executor.getGenericOperationRunners().length);
  assertEquals(groupProperties.getInteger(GroupProperty.PARTITION_OPERATION_THREAD_COUNT),executor.getPartitionThreadCount());
  assertEquals(groupProperties.getInteger(GroupProperty.GENERIC_OPERATION_THREAD_COUNT),executor.getGenericThreadCount());
}
