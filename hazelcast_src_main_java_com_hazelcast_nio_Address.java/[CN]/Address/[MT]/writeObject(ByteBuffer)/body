{
  buffer.putInt(port);
  buffer.put(type);
  if (host != null) {
    byte[] address=host.getBytes();
    buffer.putInt(address.length);
    buffer.put(address);
  }
 else {
    buffer.putInt(0);
  }
}
