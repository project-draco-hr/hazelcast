{
  logger.warning(format("Slow operation detected: %s" + "%nHint: You can enable the logging of stack traces with the following config property:" + GroupProperties.PROP_SLOW_OPERATION_DETECTOR_STACK_TRACE_LOGGING_ENABLED,log.operation));
  isFirstLog=false;
}
