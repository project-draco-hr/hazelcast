{
  logger.warning(format("Slow operation detected: %s" + "%nHint: You can enable the logging of stacktraces with the following system property: -D%s",log.operation,SLOW_OPERATION_DETECTOR_STACK_TRACE_LOGGING_ENABLED));
  isFirstLog=false;
}
