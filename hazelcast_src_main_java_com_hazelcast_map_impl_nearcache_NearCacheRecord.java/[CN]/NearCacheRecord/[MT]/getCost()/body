{
  if (!(value instanceof Data)) {
    return 0;
  }
  if (!(key instanceof Data)) {
    return 0;
  }
  return ((Data)key).getHeapCost() + ((Data)value).getHeapCost() + 2 * LONG_SIZE_IN_BYTES + LONG_SIZE_IN_BYTES + 3 * REFERENCE_COST_IN_BYTES;
}
