{
  final NodeEngine nodeEngine=mapService.getMapServiceContext().getNodeEngine();
  mapService.getMapServiceContext().getMapEventPublisher().publishEvent(nodeEngine.getThisAddress(),mapName,EntryEventType.EVICTED,key,mapService.getMapServiceContext().toData(value),null);
}
