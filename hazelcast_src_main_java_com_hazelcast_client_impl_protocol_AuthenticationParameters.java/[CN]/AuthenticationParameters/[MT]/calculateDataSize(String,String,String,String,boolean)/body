{
  return ClientMessage.HEADER_SIZE + (BitUtil.SIZE_OF_INT + username.length() * 3) + (BitUtil.SIZE_OF_INT + password.length() * 3)+ (BitUtil.SIZE_OF_INT + uuid.length() * 3)+ (BitUtil.SIZE_OF_INT + ownerUuid.length() * 3)+ BitUtil.SIZE_OF_BYTE;
}
