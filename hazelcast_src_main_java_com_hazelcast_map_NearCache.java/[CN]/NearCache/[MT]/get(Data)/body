{
  fireTtlCleanup();
  CacheRecord record=cache.get(key);
  if (record != null) {
    if (record.expired()) {
      cache.remove(key);
      updateSizeEstimator(-calculateCost(record));
      return null;
    }
    record.access();
    return record.value;
  }
 else {
    return null;
  }
}
