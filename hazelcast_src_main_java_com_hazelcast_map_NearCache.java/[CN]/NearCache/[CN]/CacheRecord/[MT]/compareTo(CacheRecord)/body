{
  if (evictionPolicy.equals("LRU"))   return ((Long)this.lastAccessTime).compareTo((o.lastAccessTime));
 else   if (evictionPolicy.equals("LFU"))   return ((Integer)this.hit.get()).compareTo((o.hit.get()));
  return 0;
}
