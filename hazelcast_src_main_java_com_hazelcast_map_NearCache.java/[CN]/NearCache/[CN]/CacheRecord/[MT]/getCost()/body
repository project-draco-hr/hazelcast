{
  if (!(value instanceof Data))   return 0;
  return key.totalSize() + ((Data)value).totalSize() + 2 * (Integer.SIZE / Byte.SIZE) + 2 * (Long.SIZE / Byte.SIZE) + (Integer.SIZE / Byte.SIZE) + 3 * (Integer.SIZE / Byte.SIZE);
}
