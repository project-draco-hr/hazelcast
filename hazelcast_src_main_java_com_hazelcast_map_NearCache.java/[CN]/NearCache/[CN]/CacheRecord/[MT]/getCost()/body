{
  return key.totalSize() + (value instanceof Data ? ((Data)value).totalSize() + 2 * (Integer.SIZE / Byte.SIZE) : 0) + 2 * (Long.SIZE / Byte.SIZE) + Integer.SIZE / Byte.SIZE + 3 * (Integer.SIZE / Byte.SIZE);
}
