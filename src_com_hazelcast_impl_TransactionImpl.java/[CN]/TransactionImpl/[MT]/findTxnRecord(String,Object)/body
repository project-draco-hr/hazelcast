{
  for (  TxnRecord txnRecord : lsTxnRecords) {
    if (txnRecord.name.equals(name)) {
      if (txnRecord.key != null) {
        if (txnRecord.key.equals(key))         return txnRecord;
      }
    }
  }
  return null;
}
