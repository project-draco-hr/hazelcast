{
  SocketWritable writable;
  if (conn.getType().equals(Connection.Type.CLIENT))   writable=createEntryEventPacket(event);
 else {
    writable=createEntryEventCommandResponse(event);
  }
  sendPacket(writable);
}
