{
  if (!source.equals(owner)) {
    throw new HazelcastException("Cannot migrate! This node is not owner of the partition => " + migrationInfo + " -> partitionId="+ partition.getPartitionId()+ " , "+ partition);
  }
}
