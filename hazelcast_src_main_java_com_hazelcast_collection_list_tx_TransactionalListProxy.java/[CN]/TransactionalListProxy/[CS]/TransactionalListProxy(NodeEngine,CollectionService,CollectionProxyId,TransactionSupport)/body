{
  super(nodeEngine,service,proxyId,tx,nodeEngine.getConfig().getMultiMapConfig("list:" + proxyId.getKeyName()).setValueCollectionType(MultiMapConfig.ValueCollectionType.LIST));
  this.key=nodeEngine.toData(proxyId.getKeyName());
}
