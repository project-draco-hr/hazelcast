{
  p.flipBuffers();
  p.read();
  p.setFromConnection(connection);
  if (p.client) {
    node.executorManager.executeLocally(new ClientRequestHandler(p));
  }
 else   clusterService.enqueueAndReturn(p);
}
