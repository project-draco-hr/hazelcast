{
  checkTime(time,timeunit);
  Protocol protocol=proxyHelper.doCommand(Command.MLOCKMAP,new String[]{getName(),String.valueOf(timeunit.toMillis(time))},null);
  return Boolean.valueOf(protocol.args[0]);
}
