{
  if (!inited.get()) {
synchronized (Node.class) {
      if (!inited.get()) {
        node=Node.get();
        node.start();
        inited.set(true);
        ManagementService.register(node.getClusterImpl());
      }
    }
  }
}
