{
  super.readData(in);
  backupCount=in.readInt();
  final boolean isData=in.readBoolean();
  if (isData) {
    Data data=new Data();
    data.readData(in);
    value=data;
  }
 else {
    value=in.readObject();
  }
}
