{
  return ClientMessage.HEADER_SIZE + ParameterUtil.calculateStringDataSize(className) + ParameterUtil.calculateStringDataSize(causeClassName)+ ParameterUtil.calculateStringDataSize(message)+ ParameterUtil.calculateStringDataSize(stacktrace);
}
