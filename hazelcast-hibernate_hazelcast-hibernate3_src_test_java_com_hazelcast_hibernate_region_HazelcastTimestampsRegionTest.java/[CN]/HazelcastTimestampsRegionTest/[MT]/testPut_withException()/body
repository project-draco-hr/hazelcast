{
  doThrow(new OperationTimeoutException("expected exception")).when(cache).put(eq("putKey"),eq("putValue"),anyLong(),any());
  region.put("putKey","putValue");
  verify(cache).put(eq("putKey"),eq("putValue"),anyLong(),any());
}
