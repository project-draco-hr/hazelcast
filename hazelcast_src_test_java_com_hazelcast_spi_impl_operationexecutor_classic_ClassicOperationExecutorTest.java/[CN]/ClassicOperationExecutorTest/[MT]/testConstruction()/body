{
  initExecutor();
  assertEquals(groupProperties.PARTITION_COUNT.getInteger(),executor.getPartitionOperationRunners().length);
  assertEquals(executor.getGenericOperationThreadCount(),executor.getGenericOperationRunners().length);
  assertEquals(groupProperties.PARTITION_OPERATION_THREAD_COUNT.getInteger(),executor.getPartitionOperationThreadCount());
  assertEquals(groupProperties.GENERIC_OPERATION_THREAD_COUNT.getInteger(),executor.getGenericOperationThreadCount());
}
