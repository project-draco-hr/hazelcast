{
  initExecutor();
  assertEquals(groupProperties.getInteger(GroupProperty.PARTITION_COUNT),executor.getPartitionOperationRunners().length);
  assertEquals(executor.getGenericOperationThreadCount(),executor.getGenericOperationRunners().length);
  assertEquals(groupProperties.getInteger(GroupProperty.PARTITION_OPERATION_THREAD_COUNT),executor.getPartitionOperationThreadCount());
  assertEquals(groupProperties.getInteger(GroupProperty.GENERIC_OPERATION_THREAD_COUNT),executor.getGenericOperationThreadCount());
}
