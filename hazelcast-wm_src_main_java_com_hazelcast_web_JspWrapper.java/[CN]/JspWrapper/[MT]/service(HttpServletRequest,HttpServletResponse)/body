{
  if (!(request instanceof RequestWrapper)) {
    HttpServletRequest reqHazel=(HttpServletRequest)request.getAttribute(WebFilter.HAZELCAST_REQUEST);
    if (reqHazel != null)     request=reqHazel;
  }
  _jspService(request,response);
}
