{
  Config config=new Config();
  config.getMapConfig("mapStore*").setMapStoreConfig(new MapStoreConfig().setWriteDelaySeconds(1000).setImplementation(mapStore));
  server=Hazelcast.newHazelcastInstance(config);
  client=HazelcastClient.newHazelcastClient(null);
}
