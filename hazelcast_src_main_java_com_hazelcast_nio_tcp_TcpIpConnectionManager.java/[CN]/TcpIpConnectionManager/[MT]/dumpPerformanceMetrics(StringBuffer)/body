{
  for (int k=0; k < inSelectors.length; k++) {
    InSelectorImpl inSelector=inSelectors[k];
    sb.append(inSelector.getName()).append(".readEvents=").append(inSelector.getReadEvents()).append("\n");
  }
  for (int k=0; k < outSelectors.length; k++) {
    OutSelectorImpl outSelector=outSelectors[k];
    sb.append(outSelector.getName()).append(".writeEvents=").append(outSelector.getWriteEvents()).append("\n");
  }
}
