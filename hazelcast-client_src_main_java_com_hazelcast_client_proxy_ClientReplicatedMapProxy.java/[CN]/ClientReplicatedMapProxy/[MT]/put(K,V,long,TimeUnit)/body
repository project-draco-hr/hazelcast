{
  final ClientReplicatedMapPutTtlRequest request=new ClientReplicatedMapPutTtlRequest(getName(),key,value,timeUnit.toMillis(ttl));
  return invokeOnPartition(request,getOrInitTargetPartitionId());
}
