{
  for (int i=0; i < MAX_REPLICA_COUNT; i++) {
    if (deadAddress.equals(addresses.get(i))) {
      addresses.set(i,null);
      for (int a=i; a + 1 < MAX_REPLICA_COUNT; a++) {
        addresses.set(a,addresses.get(a + 1));
      }
      return;
    }
  }
}
