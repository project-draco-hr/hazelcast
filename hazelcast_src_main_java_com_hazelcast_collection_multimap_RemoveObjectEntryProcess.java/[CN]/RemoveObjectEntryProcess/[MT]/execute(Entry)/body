{
  Collection coll=entry.getValue();
  if (coll == null) {
    return false;
  }
  boolean result=coll.remove(isBinary() ? data : IOUtil.toObject(data));
  if (coll.isEmpty()) {
    entry.removeEntry();
  }
  return result;
}
