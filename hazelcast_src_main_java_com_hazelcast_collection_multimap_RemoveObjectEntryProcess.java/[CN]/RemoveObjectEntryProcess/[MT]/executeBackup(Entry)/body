{
  Collection coll=entry.getValue();
  if (coll == null) {
    return;
  }
  coll.remove(isBinary() ? data : entry.getSerializationService().toObject(data));
  if (coll.isEmpty()) {
    entry.removeEntry();
  }
}
