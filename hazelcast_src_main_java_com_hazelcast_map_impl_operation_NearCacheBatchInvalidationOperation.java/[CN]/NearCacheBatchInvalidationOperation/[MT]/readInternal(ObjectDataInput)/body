{
  super.readInternal(in);
  int size=in.readInt();
  List<Data> keys=new ArrayList<Data>(size);
  for (int i=0; i < size; i++) {
    Data key=in.readData();
    keys.add(key);
  }
  this.keys=keys;
}
