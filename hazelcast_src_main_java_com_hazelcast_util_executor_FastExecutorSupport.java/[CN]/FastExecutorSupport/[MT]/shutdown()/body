{
  lock.lock();
  try {
    live=false;
    backlogDetector.interrupt();
    for (    Thread thread : threads) {
      thread.interrupt();
    }
    onShutdown();
    threads.clear();
  }
  finally {
    lock.unlock();
  }
}
