{
  if (DEBUG)   System.out.println("connect to " + address);
  if (Node.get().getThisAddress().equals(address))   throw new RuntimeException("Connecting to self! " + address);
  Connector connector=new Connector(address);
  this.addTask(connector);
}
