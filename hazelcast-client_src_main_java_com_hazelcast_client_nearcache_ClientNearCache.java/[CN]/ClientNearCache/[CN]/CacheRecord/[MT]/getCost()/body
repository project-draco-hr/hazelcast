{
  if (!(value instanceof Data)) {
    return 0;
  }
  if (!(key instanceof Data)) {
    return 0;
  }
  return ((Data)key).getHeapCost() + ((Data)value).getHeapCost() + 2 * (Long.SIZE / Byte.SIZE) + (Integer.SIZE / Byte.SIZE) + THREE_FACTOR * (Integer.SIZE / Byte.SIZE);
}
