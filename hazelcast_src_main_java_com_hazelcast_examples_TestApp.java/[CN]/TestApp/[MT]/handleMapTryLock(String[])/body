{
  String key=args[1];
  long time=(args.length > 2) ? Long.valueOf(args[2]) : 0;
  boolean locked=false;
  if (time == 0)   locked=map.tryLock(key);
 else   locked=map.tryLock(key,time,TimeUnit.SECONDS);
  println(locked);
}
