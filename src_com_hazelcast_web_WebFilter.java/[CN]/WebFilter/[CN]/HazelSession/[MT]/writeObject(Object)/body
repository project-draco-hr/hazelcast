{
  if (obj == null)   return null;
  try {
    Data data=ThreadContext.get().toData(obj);
    int size=data.size();
    if (minSize == -1 || minSize > size)     minSize=size;
    if (maxSize == -1 || maxSize < size)     maxSize=size;
    return data;
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
  return null;
}
