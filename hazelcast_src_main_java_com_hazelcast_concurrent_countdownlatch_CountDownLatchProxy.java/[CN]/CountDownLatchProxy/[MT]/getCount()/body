{
  final NodeEngine nodeEngine=getNodeEngine();
  Future f=nodeEngine.getOperationService().invokeOnPartition(CountDownLatchService.SERVICE_NAME,new GetCountOperation(name),partitionId);
  try {
    return (Integer)nodeEngine.toObject(f.get());
  }
 catch (  Exception e) {
    throw ExceptionUtil.rethrow(e);
  }
}
