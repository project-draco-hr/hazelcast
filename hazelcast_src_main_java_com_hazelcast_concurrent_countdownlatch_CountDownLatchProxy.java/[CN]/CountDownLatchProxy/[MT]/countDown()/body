{
  final NodeEngine nodeEngine=getNodeEngine();
  Future f=nodeEngine.getOperationService().invokeOnPartition(CountDownLatchService.SERVICE_NAME,new CountDownOperation(name),partitionId);
  try {
    f.get();
  }
 catch (  Exception e) {
    throw ExceptionUtil.rethrow(e);
  }
}
