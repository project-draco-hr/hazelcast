{
  try {
    return Long.parseLong(value);
  }
 catch (  final Exception e) {
    LOGGER.info(parameterName + " parameter value, [" + value+ "], is not a proper long. Default value, ["+ defaultValue+ "], will be used!");
    LOGGER.warning(e);
    return defaultValue;
  }
}
