{
  String event=ADDED;
  if (eventType == TYPE_REMOVED) {
    event=REMOVED;
  }
 else   if (eventType == TYPE_UPDATED) {
    event=UPDATED;
  }
  return "EntryEvent {" + getSource() + "} key="+ key+ ", value="+ value+ ", event="+ event;
}
