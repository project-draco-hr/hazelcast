{
  Object item=obj;
  if (item == null) {
    item=NULL;
  }
  lock.lock();
  try {
    if (response != null) {
      return false;
    }
    response=item;
    noValue.signal();
    return true;
  }
  finally {
    lock.unlock();
  }
}
