{
  if (obj == null) {
    obj=NULL;
  }
  lock.lock();
  try {
    if (response != null) {
      return false;
    }
    response=obj;
    noValue.signal();
    return true;
  }
  finally {
    lock.unlock();
  }
}
