{
  byte b=dis.readByte();
  char c=(char)b;
  while (c != '\n') {
    System.out.println(c);
    if (c != '\r')     line.put(b);
    b=dis.readByte();
    c=(char)dis.readByte();
  }
  return SocketTextReader.toStringAndClear(line);
}
