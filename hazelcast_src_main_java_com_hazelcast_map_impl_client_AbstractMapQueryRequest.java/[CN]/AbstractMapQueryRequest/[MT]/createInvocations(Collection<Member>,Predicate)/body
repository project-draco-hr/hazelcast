{
  List<Future> futures=new ArrayList<Future>(members.size());
  for (  Member member : members) {
    Future future=createInvocationBuilder(SERVICE_NAME,new QueryOperation(name,predicate,iterationType),member.getAddress()).invoke();
    futures.add(future);
  }
  return futures;
}
