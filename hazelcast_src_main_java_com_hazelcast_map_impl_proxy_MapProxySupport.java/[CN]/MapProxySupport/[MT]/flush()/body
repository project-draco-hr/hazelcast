{
  try {
    MapOperation operation=operationProvider.createMapFlushOperation(name);
    operationService.invokeOnAllPartitions(SERVICE_NAME,new BinaryOperationFactory(operation,getNodeEngine()));
  }
 catch (  Throwable t) {
    throw ExceptionUtil.rethrow(t);
  }
}
