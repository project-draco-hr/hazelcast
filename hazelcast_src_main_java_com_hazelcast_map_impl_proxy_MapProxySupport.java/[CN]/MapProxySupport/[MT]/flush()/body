{
  try {
    operationService.invokeOnAllPartitions(SERVICE_NAME,new BinaryOperationFactory(new MapFlushOperation(name),getNodeEngine()));
  }
 catch (  Throwable t) {
    throw ExceptionUtil.rethrow(t);
  }
}
