{
  try {
    Map<Integer,Object> results=operationService.invokeOnPartitions(SERVICE_NAME,operationFactory,partitions);
    return results;
  }
 catch (  Throwable t) {
    throw ExceptionUtil.rethrow(t);
  }
}
