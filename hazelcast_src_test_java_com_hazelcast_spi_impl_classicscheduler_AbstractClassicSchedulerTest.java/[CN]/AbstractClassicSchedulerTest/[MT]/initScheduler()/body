{
  groupProperties=new GroupProperties(config);
  scheduler=new ClassicOperationScheduler(groupProperties,loggingService,thisAddress,handlerFactory,responsePacketHandler,threadGroup,nodeExtension);
  return scheduler;
}
