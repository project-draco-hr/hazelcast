{
  logger.info("Starting with " + genericThreads.length + " generic operation threads and "+ partitionThreads.length+ " partition operation threads.");
  startAll(partitionThreads);
  startAll(genericThreads);
}
