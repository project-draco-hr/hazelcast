{
  if (header == null) {
    return null;
  }
  header.flip();
  if (!header.hasRemaining()) {
    return null;
  }
  final byte[] buff=new byte[header.limit()];
  header.get(buff);
  header.clear();
  return buff;
}
