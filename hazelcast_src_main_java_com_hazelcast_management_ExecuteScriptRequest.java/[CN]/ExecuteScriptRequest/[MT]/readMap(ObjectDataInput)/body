{
  int size=in.readInt();
  Map props=new HashMap(size);
  if (size > 0) {
    for (int i=0; i < size; i++) {
      Object key=in.readObject();
      Object value=in.readObject();
      props.put(key,value);
    }
  }
  return props;
}
