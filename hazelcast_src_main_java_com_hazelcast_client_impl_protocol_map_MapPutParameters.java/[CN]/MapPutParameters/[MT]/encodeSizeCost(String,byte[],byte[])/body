{
  return ClientMessage.HEADER_SIZE + (BitUtil.SIZE_OF_INT + name.length() * 3) + (BitUtil.SIZE_OF_INT + key.length)+ (BitUtil.SIZE_OF_INT + value.length)+ BitUtil.SIZE_OF_LONG+ BitUtil.SIZE_OF_LONG+ BitUtil.SIZE_OF_BYTE;
}
