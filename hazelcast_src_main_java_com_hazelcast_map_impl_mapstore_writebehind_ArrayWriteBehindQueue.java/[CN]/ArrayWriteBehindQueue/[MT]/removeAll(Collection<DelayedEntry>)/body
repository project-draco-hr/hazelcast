{
  if (collection == null || collection.isEmpty()) {
    return;
  }
  if (list.isEmpty()) {
    return;
  }
  final int size=collection.size();
  for (int i=0; i < size; i++) {
    list.remove(0);
  }
}
