{
  final int maxCharsToIndex=5;
  final char[] chars=s.toCharArray();
  long result=0L;
  for (int i=0; i < Math.min(maxCharsToIndex,chars.length); i++) {
    result+=Math.pow(127,(maxCharsToIndex - i)) * chars[i];
  }
  return result;
}
