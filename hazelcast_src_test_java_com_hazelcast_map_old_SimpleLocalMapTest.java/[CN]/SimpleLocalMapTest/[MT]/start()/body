{
  if (!instance.getCluster().getMembers().iterator().next().localMember()) {
    return;
  }
  printVariables();
  ExecutorService es=Executors.newFixedThreadPool(threadCount);
  startPrintStats();
  load(es);
  run(es);
}
