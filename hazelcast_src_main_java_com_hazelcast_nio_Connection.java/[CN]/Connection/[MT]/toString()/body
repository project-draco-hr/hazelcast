{
switch (type) {
case NONE:
    return "Connection [" + this.socketChannel.socket().getRemoteSocketAddress() + "] live="+ live+ ", client="+ isClient();
default :
  break;
}
return "Connection [" + this.endPoint + "] live="+ live+ ", client="+ isClient();
}
