{
  if (evictionChecker != null) {
    if (evictionChecker.isEvictionRequired()) {
      return evict(evictableStore,evictionPolicyEvaluator);
    }
 else {
      return 0;
    }
  }
 else {
    return evict(evictableStore,evictionPolicyEvaluator);
  }
}
