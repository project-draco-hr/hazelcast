{
  TestClientApp app=new TestClientApp();
  if (arguments.length == 0)   app.noArgument();
 else   try {
    app.connect(arguments);
    app.run(arguments);
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
}
