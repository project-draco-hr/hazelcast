{
  throwExceptionIfNull(e);
  final Data value=toData(e);
  ClientMessage request=TransactionalListRemoveCodec.encodeRequest(getName(),getTransactionId(),ThreadUtil.getThreadId(),value);
  ClientMessage response=invoke(request);
  return TransactionalListRemoveCodec.decodeResponse(response).response;
}
