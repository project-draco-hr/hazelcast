{
  throwExceptionIfNull(e);
  final Data value=toData(e);
  ClientMessage request=TransactionalListAddCodec.encodeRequest(getName(),getTransactionId(),ThreadUtil.getThreadId(),value);
  ClientMessage response=invoke(request);
  return TransactionalListAddCodec.decodeResponse(response).response;
}
