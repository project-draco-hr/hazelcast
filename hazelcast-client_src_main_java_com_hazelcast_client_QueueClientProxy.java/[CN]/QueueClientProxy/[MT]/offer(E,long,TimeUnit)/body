{
  check(e);
  checkTime(l,timeUnit);
  l=(l < 0) ? 0 : l;
  if (e == null) {
    throw new NullPointerException();
  }
  return protocolProxyHelper.doCommandAsBoolean(Command.QOFFER,new String[]{getName(),String.valueOf(timeUnit.toMillis(l))},protocolProxyHelper.toData(e));
}
