{
switch (keyType) {
case STRING:
    map.put("key" + index,index);
  break;
case INTEGER:
map.put(index,index);
break;
default :
throw new UnsupportedOperationException("Unsupported keyType " + keyType);
}
}
