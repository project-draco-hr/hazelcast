{
  if (packetProcessors[operation] != null) {
    logger.log(Level.SEVERE,operation + " is registered already with " + packetProcessors[operation]);
  }
  packetProcessors[operation]=packetProcessor;
}
