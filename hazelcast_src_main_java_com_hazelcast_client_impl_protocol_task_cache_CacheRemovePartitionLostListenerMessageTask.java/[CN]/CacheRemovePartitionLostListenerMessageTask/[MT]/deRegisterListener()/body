{
  ICacheService service=getService(CacheService.SERVICE_NAME);
  return service.getNodeEngine().getEventService().deregisterListener(ICacheService.SERVICE_NAME,parameters.name,parameters.registrationId);
}
