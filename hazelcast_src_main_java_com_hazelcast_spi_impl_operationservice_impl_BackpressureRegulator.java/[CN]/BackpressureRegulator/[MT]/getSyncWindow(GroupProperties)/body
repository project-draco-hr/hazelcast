{
  int syncWindow=props.BACKPRESSURE_SYNCWINDOW.getInteger();
  if (enabled && syncWindow <= 0) {
    throw new IllegalArgumentException("Can't have '" + props.BACKPRESSURE_SYNCWINDOW.getName() + "' with a value smaller than 1");
  }
  return syncWindow;
}
