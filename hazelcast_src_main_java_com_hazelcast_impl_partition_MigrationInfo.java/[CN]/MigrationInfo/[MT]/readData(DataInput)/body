{
  partitionId=in.readInt();
  replicaIndex=in.readInt();
  move=in.readBoolean();
  boolean hasFrom=in.readBoolean();
  if (hasFrom) {
    from=new Address();
    from.readData(in);
  }
  to=new Address();
  to.readData(in);
}
