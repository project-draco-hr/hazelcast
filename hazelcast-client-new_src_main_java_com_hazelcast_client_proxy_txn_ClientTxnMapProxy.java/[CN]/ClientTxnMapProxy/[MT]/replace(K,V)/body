{
  ClientMessage request=TransactionalMapReplaceCodec.encodeRequest(getName(),getTransactionId(),ThreadUtil.getThreadId(),toData(key),toData(value));
  ClientMessage response=invoke(request);
  return (V)toObject(TransactionalMapReplaceCodec.decodeResponse(response).response);
}
