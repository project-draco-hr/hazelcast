{
  ClientMessage request=TransactionalMapRemoveIfSameCodec.encodeRequest(getName(),getTransactionId(),ThreadUtil.getThreadId(),toData(key),toData(value));
  ClientMessage response=invoke(request);
  return TransactionalMapRemoveIfSameCodec.decodeResponse(response).response;
}
